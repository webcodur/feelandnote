
CLI 성공하는 방식, 실패하는 방식 적어둬라. 
매번 똑같은 실수 방지하게.
니가 실패를 감지하진 못하니 내가 중단시켰다가 다시 말걸면 실패했기 때문이라고 보면 된다.

## CLI 실패하는 방식
* `grep -rn` 또는 `find -exec grep` 처럼 하위 디렉토리를 깊게, 반복적으로 탐색하는 무거운 명령어는 Windows bash 환경에서 타임아웃/멈춤 현상(Hang)을 유발함.
* `dir /s /b` 같은 Windows cmd 명령어를 Bash 쉘에서 실행하려 하면 `/s` 등의 플래그를 파일 경로로 인식해 오류 발생.
* `node script.js`로 다수의 큰 파일(800KB×12) `fs.copyFileSync` 도 행 발생. 파일 복사는 개별 `cp` 명령 or 소량씩 분할.

## CLI 성공하는 방식
* 파일 검색, 내용 찾기는 무조건 제공된 전용 툴(`list_dir`, `grep_search` 등) 사용을 최우선으로 할 것.
* 복잡한 파일/텍스트 처리나 파이프라이닝이 필요한 경우에는 쉘 스크립트보다 Node.js 스크립트(가령 `fs` 모듈 사용)를 간단히 작성해 `node script.js`로 실행하는 것이 훨씬 안전하고 일관되게 성공함.