# 다국어화(i18n) 구현 계획

> **최종 실측 체크: 26.07.27** — 사용자 웹 정적 문구, 활성 인물 DB 번역 쌍, 인물 상세 KO/EN 실화면을 전수·표본 교차 점검했다.

---

## 1. 현황 요약

| 항목 | 상태 |
|------|------|
| Next.js | 16.1.1 (App Router) |
| i18n 라이브러리 | `next-intl` v4.8.3 ✅ |
| 라우팅 | `app/[locale]/` 구조, `localePrefix: 'as-needed'` ✅ |
| UI 텍스트 언어 | KO/EN 메시지 키 2,970개 대응, 정적 호출·하드코딩 엄격 감사 통과 |
| DB 번역 데이터 | **~33,000 단위 완료** (Phase 5~6, 9) |
| DB 구조 변경 | contents.id UUID 전환 완료, `content_locales` 테이블 분리 완료 (Phase 3 DROP 포함) |
| 남은 작업 | 개발 중인 게임 UI·가상독백 편집대는 26.07.27 사용자 지시에 따라 이번 배포에서 제외 |

---

## 2. 기술 선택

- **라이브러리**: `next-intl` (App Router 네이티브, RSC/SSR 완전 호환)
- **라우팅**: 경로 기반 (`/ko/explore`, `/en/explore`)
- **기본 언어**: `ko` (`localePrefix: 'as-needed'`)
- **DB 다국어 방안**: `content_locales` 테이블 (PK: content_id + locale). 기존 방안 A(컬럼 추가)에서 테이블 분리로 전환 완료.

---

## 3. 완료된 작업 요약

### DB 스키마 (Phase 5) ✅

| 테이블 | 추가 컬럼 |
|--------|----------|
| `profiles` | `title_en`, `bio_en`, `cultural_journey_en` (`quotes_en`은 당시 추가됐으나 2026-03-23 `drop_profiles_quotes_and_recreate_compat_view`로 `quotes`와 함께 DROP — 현재 없음) |
| `celeb_influence` | `*_exp_en` × 7 |
| `celeb_dialogues` | `lines_en` (JSONB) |
| `celeb_tags` | `name_en`, `description_en` |
| `celeb_tag_assignments` | `short_desc_en`, `long_desc_en` |
| `contents` | `external_id` (유지) |
| `content_locales` | (content_id, locale) PK. title, creator, thumbnail_url, isbn, publisher, description, affiliate_url — **contents의 i18n 컬럼 7개 DROP 완료** |

### DB 번역 데이터 (Phase 6) ✅

| 항목 | 규모 | 완료 |
|------|------|------|
| profiles (title, bio) + 명언 | 867명 × 3 | ✅ (명언 정본은 이후 `celeb_dialogues.lines_en.quote`로 이관) |
| profiles (nickname_en 보충) | 37명 | ✅ |
| profiles (cultural_journey_en) | 867명 | ✅ |
| celeb_influence (*_exp_en) | 867명 × 7 | ✅ |
| celeb_dialogues (lines_en) | 867명 × 21대사 | ✅ |
| celeb_tags / assignments | 12 + ~45건 | ✅ |
| contents title_ko/title_en 분배 + API 보충 | 6,304건 | ✅ |
| scriptures JSON 영문 번역 | 4파일 (2,177줄) | ✅ |

### contents.id UUID 전환 (Phase 9.1) ✅

| 작업 | 상태 |
|------|------|
| external_id 컬럼 추가 + 기존 id 복사 | ✅ |
| FK 해제 → UUID 변환 → FK 재생성 | ✅ |
| id 기본값 gen_random_uuid() 설정 | ✅ |
| 코드 14개 파일 수정 (addContent, getContentDetail 등) | ✅ |
| isbn_ko 컬럼 추가 + 한국 ISBN 복사 | ✅ |

### web-bo 어드민 ✅

모든 _en 필드에 대해 조회·편집 UI 추가 완료:
- CelebForm: nickname_en, title_en, bio_en, quotes_en, cultural_journey_en
  - `quotes`/`quotes_en`은 폼 입력 필드명일 뿐 profiles 컬럼이 아니다. `actions/admin/celebs.ts`가 이 값을 `celeb_dialogues.lines.quote`·`lines_en.quote`에 기록한다(SSoT).
- DialogueEditor: lines_en (KO/EN 토글)
- TagFormModal / TagAccordionItem: name_en, description_en, short_desc_en, long_desc_en
- InfluenceDashboard: *_exp_en 표시
- celebs/[id] 상세: cultural_journey_en, DialogueSection (KO/EN 토글)

### 프론트 인프라 (Phase 0) ✅ — 2026-03-02

| 파일 | 작업 | 비고 |
|------|------|------|
| `sw/web/src/i18n/routing.ts` | 신규 | `locales: ['ko','en']`, `localePrefix: 'as-needed'` |
| `sw/web/src/i18n/request.ts` | 신규 | 서버 요청 locale + messages 로딩 |
| `sw/web/src/i18n/navigation.ts` | 신규 | locale-aware Link, useRouter 등 |
| `sw/web/messages/ko/common.json` | 신규 | 한국어 번역 키 (~200개) |
| `sw/web/messages/en/common.json` | 신규 | 영문 번역 |
| `sw/web/src/app/[locale]/layout.tsx` | 신규 | metadata, NextIntlClientProvider, Footer, GA |
| `sw/web/next.config.ts` | 수정 | `withNextIntl` 플러그인 래핑 |
| `sw/web/src/proxy.ts` | 수정 | next-intl middleware + Supabase 세션 통합, `api/` 제외 |
| `sw/web/src/app/layout.tsx` | 수정 | shell로 경량화 (폰트 + `getLocale()` 동적 lang) |
| `sw/web/src/app/[locale]/not-found.tsx` | 이동+수정 | `useTranslations('notFound')` 다국어 적용 |
| route groups 7개 | 이동 | `app/(main)` 등 → `app/[locale]/(main)` 등 |

**핵심 설계:**
- `localePrefix: 'as-needed'` → 한국어 URL 무변경 (`/explore`), 영어만 `/en/explore`
- `proxy.ts`에서 next-intl middleware와 Supabase `updateSession` 쿠키 통합
- `api/`, `auth/callback` 은 locale routing 제외

### 번역 키 추출 (Phase 1) ✅ — 2026-03-02

| 네임스페이스 | 키 수 | 소스 상수 파일 |
|-------------|-------|--------------|
| `nav` | ~20 | `navigation.tsx` |
| `home` | ~15 | `navigation.tsx` (HOME_SECTIONS) |
| `banner` | 2 | `navigation.tsx` (PAGE_BANNER) |
| `content` | ~25 | `categories.ts` |
| `status` | ~10 | `statuses.ts` |
| `profession` | ~17 | `celebProfessions.ts` (shared) |
| `title` | ~40 | `titles.ts` |
| `review` | ~70 | `review-presets.ts` |
| **합계** | **~200** | |

- `filterStyles.ts`: 한국어 텍스트 없음 (CSS 클래스만), 스킵
- `materials.ts`: 이미 `{ en, ko }` 구조 내장, JSON 분리 불필요, 스킵
- 상수 파일 자체는 미수정 — Phase 2에서 컴포넌트 전환 시 함께 리팩터링

---

## 4. 데이터 정비 (Phase 9) — 완료

> ⚠️ 아래 섹션은 `content_locales` 마이그레이션 이전 컬럼명(`isbn_ko`, `isbn_en`, `title_ko` 등)을 사용한다.
> 현재 이 데이터는 모두 `content_locales` 테이블로 이관 완료.

### 9.2 isbn_ko 매칭 ✅

영어 ISBN이 external_id인 BOOK 중 한국어 판본 ISBN 확보.

| 방법 | 건수 | 상태 |
|------|------|------|
| 마이그레이션: 한국 ISBN(978-8/979-11) → isbn_ko 복사 | 1,674 | ✅ |
| Naver API 매칭 (title_ko → 한국 ISBN 검색) | 238 | ✅ |
| 매칭 불가 (미출판/절판/제목 불일치) | 314 | 정상 빈값 |
| title_ko 영어 오염 → null 정리 | 17 | ✅ |
| 제목 오염 정리 (양장본 Hardcover 등) | 4 | ✅ |
| **합계 isbn_ko 보유** | **1,912 / 2,708** (71%) | |

### 9.3 contents 제목 현황 ✅

| 타입 | 전체 | title_ko | title_en |
|------|------|----------|----------|
| BOOK | 2,708 | 2,250 (83%) | 2,505 (93%) |
| VIDEO | 1,340 | 1,276 (95%) | 1,340 (100%) |
| MUSIC | 1,473 | 117 (8%) | 1,471 (100%) |
| GAME | 101 | 12 (12%) | 101 (100%) |

미보유 잔여: BOOK title_en ~203건(한국 고유 저작), BOOK title_ko ~458건(한국어 번역본 없음), MUSIC/GAME title_ko 대부분(영어권 콘텐츠) — 모두 **정상 빈값**.

### 9.4~9.5 isbn_en / creator_en 매칭

→ **완료**(26.03). naver_book 2,364건 전량 verified, 한글/CJK 잔존 0건. 진단·수정 이력은 **`docs/archive/en-book-data-quality.md`** 참조.

---

## 5. 남은 작업

### Phase 0: 프론트 인프라 구축 ✅

- [x] `next-intl` v4.8.3 설치
- [x] `i18n/routing.ts`, `i18n/request.ts`, `i18n/navigation.ts` 작성
- [x] `proxy.ts` (next-intl middleware + Supabase 세션 통합)
- [x] `app/[locale]/layout.tsx` 생성 (metadata, NextIntlClientProvider, Footer, GA)
- [x] `app/layout.tsx` → shell로 경량화 (폰트 + getLocale() 동적 lang)
- [x] 기존 라우트 7개 그룹을 `app/[locale]/` 하위로 이동
- [x] `not-found.tsx` 다국어 적용 (useTranslations)
- [x] `next.config.ts` withNextIntl 플러그인 래핑
- [x] `localePrefix: 'as-needed'` — 한국어 URL 무변경, 영어만 `/en/` prefix

### Phase 1: 상수 파일 번역 키 추출 ✅

- [x] `messages/ko/common.json` — 전체 상수 번역 키 추출 (~200개)
- [x] `messages/en/common.json` — 영문 번역 완료
- [x] 네임스페이스: nav, home, banner, content, status, profession, title, review
- [x] 상수 파일 자체는 미수정 (Phase 2에서 컴포넌트 전환 시 함께 리팩터링)

**추출 대상:**

| 파일 | 텍스트 수 | 내용 |
|------|----------|------|
| `constants/navigation.tsx` | ~15 | 섹션 라벨 (기존 `englishTitle` 활용) |
| `constants/categories.ts` | ~5 | 콘텐츠 카테고리 |
| `constants/statuses.ts` | ~6 | 상태 라벨 |
| `constants/titles.ts` | ~25 | 칭호 |
| `constants/filterStyles.ts` | ~10 | 필터 라벨 (실제 한국어 없음, 스킵) |
| `constants/materials.ts` | ~100+ | 대량 UI 텍스트 |
| `constants/review-presets.ts` | ~50+ | 리뷰 템플릿 |

### Phase 2: 핵심 페이지 변환 ✅ — 2026-03-02

**전략**: 상수 파일 자체의 `label` 제거가 아니라, 컴포넌트에서 `t()` 호출로 전환. 상수의 `key`/`id` 필드를 번역 키로 활용.

#### 2.1 레이아웃 컴포넌트 ✅

- [x] Header.tsx: `useTranslations()`, nav 라벨 + "감상 모드"
- [x] HeaderNotifications.tsx: `useTranslations('layout.notifications')` + date-fns locale 분기
- [x] HeaderProfileMenu.tsx: `useTranslations('layout.profile')`
- [x] BottomNav.tsx: `useTranslations('nav')`
- [x] Footer.tsx: `getTranslations()` (서버 컴포넌트)
- [x] navigation.tsx: `NavSubLink.key` 필드 추가

#### 2.2 인증 페이지 ✅

- [x] login/page.tsx, EmailLoginForm.tsx, login/layout.tsx
- [x] signup/page.tsx, SignupForm.tsx
- [x] reset-password/page.tsx, reset-password/layout.tsx

#### 2.3 탐색 페이지 ✅

- [x] explore/page.tsx, layout.tsx, celebs/page.tsx, people/page.tsx

#### 2.4 메타데이터 전환 ✅

`export const metadata` → `generateMetadata()` + `getTranslations` 전환 완료:

- [x] `[locale]/layout.tsx` (루트 — site.title/description, OG locale 분기)
- [x] `(main)/page.tsx` (홈)
- [x] agora: page, celeb-feed, friend-feed, board/feedback(목록·작성·수정), board/notice(목록·작성·수정), board/free
- [x] scriptures: page, figure, era, history, profession
- [x] rest: page, dawn, labyrinth, hegemony, suikoden
- [x] notifications/layout, search/layout
- [x] policy: terms, privacy
- [x] about/page
- [x] [userId]: chamber, merits, collections, collection/[id], tiers
- 스킵: (auth)/layout (robots only), reading/layout (robots only), lab/* (영어 전용)

#### 2.5 번역 JSON 추가 키 ✅

총 ~90개 키 추가 (site, pages, agora, scriptures, rest, policy 네임스페이스)

### Phase 3: 서고 코드 연동 ✅ — 2026-03-02

- [x] scriptures JSON 영문 번역 (`constants/scriptures/en/`) ✅
- [x] `constants/scriptures/` → `ko/`, `en/` 하위 구조 재편 (git mv)
- [x] `scripturesHistory.ts` → `getScripturesData(locale)` 함수 + `HISTORY_CATEGORY_IDS` 구조체
- [x] `HISTORY_CATEGORIES` 메타데이터 → `messages/*/common.json` (`scriptures.history.category`, `scriptures.history.sub`)
- [x] `ContentHistoryTimeline.tsx` → `useLocale()` + `useTranslations()` 적용

### Phase 4: 컴포넌트 UI 텍스트 변환 — 완료

**전략**: 컴포넌트 그룹별로 `useTranslations()` 적용. 서브 컴포넌트는 `t` prop 패턴 사용.

| # | 그룹 | 파일 수 | 한국어 문자열 | 상태 |
|---|------|--------|-------------|------|
| 4.1 | `features/user/explore` | 17 | ~80 | ✅ 완료 |
| 4.2 | `features/home` | 16 | ~49 | ✅ 완료 |
| 4.3 | `features/quickRecord` | 15 | ~80 | ✅ 완료 |
| 4.4 | `features/user/profile` | 9 | ~40 | ✅ 완료 |
| 4.5 | `features/content` | 8 | ~44 | ✅ 완료 |
| 4.6 | `features/recommendations` | 3 | ~22 | ✅ 완료 |
| 4.7 | `features/profile` | 6 | ~18 | ✅ 완료 |
| 4.8 | `components/layout` | 5 | ~30 | ✅ 완료 |
| 4.9 | `features/board` | 9 | ~45 | ✅ 완료 |
| 4.10 | `features/landing` | 3 | ~5 | ✅ 완료 |
| 4.11 | `features/figure` | 1 | ~12 | ✅ 완료 |
| 4.12 | `components/shared` | 0 | 0 | ✅ 스킵 (유저 노출 문자열 없음) |
| 4.13 | `features/scriptures` | 0 | 0 | ✅ 스킵 (유저 노출 문자열 없음) |
| 4.14 | `app/` 페이지 | 24 | ~106 | ✅ 완료 |

**app/ 페이지 내역** (4.14):
- `reading/` 독서 모드: ~150건 (가장 대량)
- `(standalone)/search`: ~14건
- `(main)/[userId]`: ~12건
- `(main)/celeb/[slug]`: ~8건
- `(main)/notifications`: ~4건
- `lab/`: 스킵 (영어 전용)

**제외 대상:**
- `features/agora`: 0건 (완료됨)
- `features/book`, `features/influence`: 0건 (한국어 없음)

### Phase 5~6: DB — ✅ 완료 (위 "완료된 작업" 참조)

### Phase 7: 게임 & 특수 영역

- [ ] 천도 게임 UI 텍스트 (`features/game/` ~286건)
- [ ] `defaultLines` (톤별 범용 대사) 번역
- [x] Server Actions 오류 코드를 클라이언트 locale에서 표시

> 26.07.27: 개발 중인 게임과 가상독백 편집대는 사용자 지시에 따라 커밋·배포 대상에서 제외했다. 공용 메시지 키가 준비돼 있어도 게임 실행 코드 완료로 간주하지 않는다.

### Phase 8: QA & 마무리

- [ ] 언어 전환 UI (설정 또는 헤더)
- [x] 누락 번역 키·상세 데이터·실화면 검출 — `audit-web-i18n` 스킬
  - `pnpm audit:i18n`: KO/EN 번들·ICU·사용 키·하드코딩 정적 검사
  - `pnpm audit:i18n:data -- --slugs=<slug>`: 셀럽 상세 DB 번역 쌍 검사
  - `pnpm audit:i18n:routes -- --slugs=<slug>`: KO/EN × PC/모바일 메타·앵커·overflow 검사
- [ ] 번들 크기 확인
- [ ] SEO (hreflang, sitemap)
- [ ] 브라우저 언어 자동 감지

#### 26.07.27 검증 스냅샷

- 정적 엄격 감사: KO/EN 각 2,970개 메시지 키, 정적 번역 호출 1,988건, 오류 0, 경고 0
- 활성 인물 DB 감사: CELEB 프로필 1,526명, 프로필·영향력·자질·대사·타임라인·관계·세력·감상 경위 18개 범주 누락 0
- 상세 화면 감사: `stanley-kubrick`의 KO/EN × 1440px/390px 4화면, route·locale·SEO·앵커·가로 넘침 오류 0
- 화면 경고 20건은 카드 안의 긴 출처 URL을 의도적으로 줄인 항목이다. 링크의 `title`과 실제 `href`로 전체 주소를 확인할 수 있게 했다.
- TypeScript 검사와 `pnpm build:web` 프로덕션 빌드를 통과했다.

활성 인물만 다시 검사할 때는 다음 명령을 쓴다.

```bash
pnpm audit:i18n
pnpm audit:i18n:data -- --all --active --strict
pnpm audit:i18n:routes -- --slugs=stanley-kubrick --screenshots=.artifacts/i18n-audit
```

번역 누락을 실제로 채우는 스크립트는 감사기와 분리한다.

- `sw/web-bo/scripts/backfill-celeb-i18n.mjs`: 관리형 인물 데이터의 KO→EN 누락 보강
- `sw/web-bo/scripts/repair-celeb-dialogue-shape.mjs`: KO/EN 대사 JSON 키 구조 교정

---

## 6. user_contents 번역 ✅

### 규모

- 8,826건 (셀럽 감상평, 10자 이상 유의미 리뷰)

### 완료 내역

- [x] contents 타이틀 양방향 매칭 완료
- [x] review_en 컬럼 마이그레이션
- [x] Claude Sonnet 에이전트 병렬 번역 (4~6기 동시 운용)
- [x] **8,826건 전량 번역 완료** (2026-03-02)

---

## 7. 콘텐츠 표시 설계

### ContentCard 두 벌 표시

- locale에 따라 해당 언어 제목 우선 표시
- 양쪽 보유 시 언어 전환 토글 제공
- 한쪽만 보유 시 해당 언어 고정

### 어필리에이트 링크 (검토 중)

| locale | 플랫폼 | 대상 | 상태 |
|--------|--------|------|------|
| `ko` | 쿠팡 | 한국 ISBN 도서 | 미검토 |
| `en` | Amazon Associates | isbn_en 보유 도서 | 가입 가능 확인 (Payoneer 필요) |

- Amazon Associates: 한국 거주자 가입 가능. TIN으로 주민등록번호 사용, 수익은 Payoneer로 수령.
- isbn_en 확보 완료 후 어필리에이트 링크 생성 가능.
- `affiliate_url` JSONB에 `locale` 필드 추가 예정.

---

## 8. 기술 참조

### 서버/클라이언트 번역

| 유형 | 방법 |
|------|------|
| Server Component | `const t = await getTranslations('ns')` |
| Client Component | `const t = useTranslations('ns')` |
| Server Action | `const t = await getTranslations('ns')` |
| Metadata | `generateMetadata` 내 `getTranslations` |

### 번역 키 컨벤션

```
{네임스페이스}.{섹션}.{키}
예: nav.section.explore, content.category.book, auth.login.submitButton
```

### DB 데이터 분류

| 분류 | 전략 |
|------|------|
| UGC (방명록, 댓글) | 번역 안 함 |
| 관리형 (셀럽 bio, 대사) | `*_en` 컬럼 (방안 A) |
| 외부 API (contents) | `title_ko` + `title_en` 분리 |
| 시스템 메시지 (알림) | 생성 시 locale 반영 |
| UI 텍스트 | `next-intl` 번역 파일 |

---

## 9. 위험 요소

| 위험 | 대응 |
|------|------|
| `[locale]` 라우트 이동 → 기존 URL 깨짐 | middleware 리다이렉트, 301 |
| 번역 파일 비대화 | 네임스페이스 분할, 동적 로딩 |
| SEO 기존 인덱싱 유실 | hreflang 태그, sitemap |
| user_contents 8,825건 번역 비용 | 우선순위 분류, 단계적 처리 |
