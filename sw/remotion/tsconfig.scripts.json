{
  "//": "scripts/ 는 tsconfig.json 의 include(src·episodes) 밖이라 `tsc --noEmit` 이 검사하지 않는다. DB 이관 도구(팩션·담화)를 따로 검사하는 설정. 'wav' 패키지는 타입 선언이 없어 scripts/lib/wav.d.ts 의 ambient 선언으로 받는다.",
  "extends": "./tsconfig.json",
  "include": ["scripts/discourse", "scripts/faction", "scripts/lib"]
}
