# 가상 독백 전수 품질 정비

> 시작: 2026-07-29 · 상태: **실존 full·light 한국어 전수 완료 — 활성 1,476명 / 유지 624명 / 게시 139명 / 보류 713명**
> 한국어 원천: `profiles.virtual_monologue`  
> 실존 인물 작성 모델: GPT-5.6 · 신화·허구 인물: `fiction-profile-monologue` 별도 트랙
> 2026-07-30 완료 기록: `docs/todo/virtual-monologue-handoff-2026-07-30.md`

## 완료 상태와 후속

다음 작업자는 이 절부터 읽는다.

1. 실존 `full`·`light` 활성 프로필은 **1,476명 전원** 판정됐다. 최종 상태는 `keep/reviewed 624`, `published 139`, `hold 713`이며 `unreviewed 0`, `draft 0`, `approved 0`이다.
2. 완료 직전 최종 감사에서 누락·중복·추가 실존 인물·원문 해시 불일치·상태 불일치·닉네임 불일치가 모두 0이었다.
3. 게시 139명은 모두 원문 SHA-256 조건부 UPDATE, 캐시 갱신 HTTP 200, 같은 게시 명령 재실행 `SKIP`, 공개 한국어 HTML 문단 완전 일치를 통과했다.
4. `VM-KO-05`는 `published 9 / hold 16`, `VM-KO-06`은 `keep 6 / published 1 / hold 18`로 마감됐다.
5. 남은 1,303명은 임시 배치 `VM-KO-T-01`~`14`에서 빠른 텍스트 판정했다. `keep 618 / published 55 / hold 630`이며, 빠른 `keep`은 외부 사실 인증이 아니라 현행 원고를 유지해도 된다는 텍스트 편집 판정이다. 완료 뒤 수치와 판단을 이 문서에 흡수하고 임시 배치·실행 이력·일회성 러너는 사용자 요청에 따라 삭제했다.
6. 빠른 트랙은 모델 호출을 전역 4개로 제한하면서 서로 다른 배치 2개를 병렬 처리하고, 같은 파일은 단계별 원자 병합한다. 1,303명 전수 판정 뒤 개선 필요자만 후보를 생성해 이전의 전원 dossier 방식보다 호출량을 줄였다.
7. 빠른 개선 후보도 사실 비증가와 한국어 편집의 두 독립 검토를 통과해야 한다. 모델 동시 통과 뒤에도 사람 직접 통독에서 14명을 추가 보류했으며 기존 DB 원문을 보존했다.
8. 한 후보 생성 실패(르네 데카르트)는 전체 배치를 막지 않고 대상만 보류했다. 근거 밖 직접 인용을 반복 생성한 것이 원인이며 실패 이유와 재개 조건은 배치에 남아 있다.
9. 손빈 프로필의 생몰일 `출생 기원전 316 / 사망 기원전 342` 선후 역전은 독백 밖의 별도 프로필 교정 대상이다.
10. 다음 범위는 `fiction-profile-monologue` 전용 원전 트랙과 `virtual_monologue_en` 후속이다. 두 범위를 실존 한국어 완료 수치에 섞지 않는다.

## 왜 별도 작업인가

기존 독백은 인물별 말투와 구성, 어려운 정보의 독자 안내, 삶과 철학의 연결을 전수 비판한 산출물이 아니다. 현행 실존 인물 생성기 `fill-virtual-monologue-gpt.ts`도 다음 한계가 있다.

- 근거 재료가 `bio + 생몰`뿐이다.
- 기존 글이 가진 근거 없는 동기·내면·영웅 서사를 개선본이 물려받을 수 있다.
- 기존 글의 재사용 가치와 무관하게 한 방식으로 처리한다.
- 생성 직후 독립적인 사실·구조·한국어 검토가 없다.
- 2026-07-29 착수 시점에는 생성 결과를 곧바로 DB에 UPDATE했다.

문법 교정기 `audit-monologue-grammar.ts`는 비문만 국소 치환하는 도구다. 이 작업이 다루는 사실, 인물성, 구조, 난이도, 근거 밖 자기고백은 검수 범위가 아니다.

## 2026-07-29 기준선

읽기 전용 재현:

```bash
cd sw/web-bo
pnpm exec tsx scripts/audit-public-celeb-quality.ts --json
```

Windows PowerShell 실행 정책이 `pnpm.ps1`을 막으면 `pnpm.cmd`를 사용한다.

- 활성 CELEB 1,527명
  - 전용 감사 시점(2026-07-29 20:58 KST): full 1,301 · light 175 · fiction 48 · relation 3
- full+light 1,476명
- full+light 독백 결측 0명
- full+light 독백 700자 미만 2명
- 한국어 bio 80자 미만 1,116명

짧다는 사실은 결함 판정이 아니다. 특히 bio가 얇으므로 bio만으로 독백의 사실 안전성을 주장할 수 없다는 것이 중요한 기준선이다.

### 전용 구조 감사 결과

재현:

```bash
cd sw/web-bo
node --env-file=.env --import tsx scripts/audit-virtual-monologues.ts \
  --out ../../docs/celeb-data/virtual-monologue/2026-07-29-structural-audit.json
```

산출물: `docs/celeb-data/virtual-monologue/2026-07-29-structural-audit.json`

- 1,527명 전수 조회. 1,000행 상한을 넘어 두 페이지를 읽었다.
- 독백 없음 1명: relation 티어 `clowwindy`. relation 화면에서 독백이 필요한지 확인하기 전에는 임의 생성하지 않는다.
- URL·Markdown 링크 혼입 3명: 히파티아·페리클레스·손빈
- 700자 미만 2명
- 첫 문장에 자기 이름이 나오는 인물 883명
- 괄호 안내가 하나라도 있는 인물 3명
- 근거 대조가 필요한 내면 표현 정규식 신호 179명
- 다른 인물과 같은 5단어 연속 구절을 가진 인물 61명, 공유 구절 25종
- 구조상 blocking 4명, 고우선 검토 6명, 중간 신호 1,147명, 신호 없음 370명

반복 상위에는 `<인물>라는 이름을 들으면 사람들은 먼저`, `그 말을 들을 때마다`, `그 말이 아주 틀렸다고 우길 생각은 없습니다`가 있었다. 이는 판박이 구조의 실제 증거다.

`structuralPriority`는 리서치 순서다. 정규식에 걸리지 않은 370명이 좋은 글이라는 뜻이 아니며, 내면 표현 179명도 그 표현만으로 오류가 확정된 것은 아니다.

### GPT 작성 모드 소규모 실험

피터 틸·알렉스 카프·마크 안드레센에게 GPT-5.6으로 `기존 글 개선`과 `근거 기반 신규 작성`을 각각 요청해 블라인드 비교했다.

- 피터 틸: 개선본 우세
- 알렉스 카프: 신규본 우세
- 마크 안드레센: 개선본 우세

표본은 세 명뿐이며 어느 모드의 보편적 우월성을 증명하지 않는다. 결론은 **기존 글의 재사용 가치에 따라 분기해야 한다**는 것이다.

## 절대 불변 규칙

1. **좋은 글은 다시 생성하지 않는다.** 모든 인물을 바꾸는 것이 목표가 아니다.
2. **생성기는 후보만 만든다.** 별도 승인·조건부 게시기를 거치기 전 DB UPDATE 금지.
3. **기존 글은 개선 모드에만 제공한다.** 신규 모드는 기존 표현과 구조를 보지 않는다.
4. **외부 비판을 당사자의 자기반성으로 바꾸지 않는다.**
5. **공개 행동을 단일한 내적 동기의 결과로 확정하지 않는다.**
6. **출처 없는 현장 장면·군중 반응·구체 수치·인과를 만들지 않는다.**
7. **직접인용은 원문과 출처가 있을 때만 쓴다.**
8. **어려운 말은 먼저 쉬운 말로 바꾼다.** 용어 보존이 필요할 때만 `용어(쉬운 뜻)`으로 안내한다.
9. **괄호 개수를 품질 지표로 삼지 않는다.** 필요 없는 괄호는 설명이 아니라 소음이다.
10. **한국어를 먼저 확정한다.** 영문은 같은 사실과 무게를 바탕으로 별도 작성한다.
11. **실존 인물과 fiction을 섞지 않는다.** fiction은 원전 manifest와 전용 스킬을 따른다.
12. **수치 점수로 승인하지 않는다.** `blocking / major / minor` 결함과 통독 판단으로 결정한다.

## 네 갈래 판정

### 유지

- 핵심 사실이 근거와 맞는다.
- 인물 고유의 중심 갈등과 문장 호흡이 이미 있다.
- 어려운 정보가 초심자를 버리지 않는다.
- blocking·major 결함이 없다.

정밀 dossier 트랙의 유지 판정은 근거 대조를 거친다. 다만 2026-07-30부터
도입한 빠른 전수 판정의 `keep`은 **본문에서 보이는 구조·문장·인물성 결함이
없어 기존 공개 원고를 바꾸지 않는 편집 판정**이다. 이는 역사 사실을 새로
인증했다는 뜻이 아니며, 후보 생성·승인·DB 쓰기를 하지 않는다. 본문에서
근거 밖 내면·인과·가상 참회가 의심되면 keep으로 두지 않고 hold로 보낸다.

### 개선

- 검증 가능한 생애와 사상의 중심축이 남아 있다.
- 좋은 문장과 인물 고유 재료를 특정할 수 있다.
- 결함이 일부 문단의 과밀함, 난이도, 순서, 말투처럼 국소적이다.
- 기존 글의 뼈대를 보존해도 근거 밖 자기서사가 원고를 지배하지 않는다.

### 신규

- 판박이 자기소개·업적 나열·교훈형 결말이 글의 뼈대다.
- 근거 없는 동기·깨달음·후회·미해결 고백이 중심 역할을 한다.
- 서로 무관한 개념을 많이 넣어 인물의 삶과 철학이 연결되지 않는다.
- 비슷한 인물 이름으로 바꿔도 대부분 성립한다.
- 국소 수정으로 살리면 사실상 전면 재작성이 된다.

### 보류

- 신원이나 생애 정보가 충돌한다.
- 믿을 만한 직접·기관·전기 자료가 부족하다.
- 논쟁적 사실을 한 출처로만 확인할 수 있다.
- 본인과 동명이인, 공동창업자, 작품 속 인물의 발언이 분리되지 않는다.

자료가 없으면 짧게 쓰는 것이 아니라 작성 자체를 보류한다.

## 인물별 근거 묶음

실존 인물 후보 생성 전에 다음 구조를 채운다.

```json
{
  "slug": "peter-thiel",
  "identity": {
    "name": "피터 틸",
    "birth": "1967-10-11",
    "death": null,
    "disambiguation": "PayPal·Palantir 공동창업자"
  },
  "sources": [
    {
      "url": "https://example.com/original",
      "kind": "first_party",
      "supports": ["확인한 사실 또는 주장"]
    }
  ],
  "turningPoints": ["검증된 생애 전환점"],
  "positions": ["본인이 공개적으로 밝힌 주장"],
  "tensions": [
    {
      "claim": "지우면 안 되는 외부 비판 또는 모순",
      "speakerOwnsThisCriticism": false
    }
  ],
  "voice": {
    "sentenceLength": "short|mixed|long",
    "directness": "low|medium|high",
    "moves": ["실제 발화 자료에서 확인한 논리 전개"],
    "avoid": ["이 인물에게 붙이면 안 되는 창작 말버릇"]
  },
  "hardTerms": [
    {
      "term": "모방 욕망",
      "plainGuide": "남이 원하는 것을 따라 원하게 되는 심리",
      "essential": true
    }
  ]
}
```

출처 우선순위:

1. 본인 저술·서한·연설·인터뷰·공식 SNS
2. 공식 기관·대학·재단·기업의 원문
3. 학술 자료·평전·박물관·기록관
4. 신뢰도 높은 보도
5. 출처 없는 어록·요약·3자 큐레이션은 근거로 쓰지 않음

`consumption_philosophy`, `celeb_persona`, `celeb_dialogues`, 기존 가상 담화는 근거 묶음의 사실 원천으로 쓰지 않는다. 이 데이터도 별도 정비 대상이므로 오류가 독백으로 전이될 수 있다.

## 작성·검토 순서

1. **구조 감사** — 전수 텍스트의 결측, 링크·한자·형식 잔존, 판박이 오프닝·결말, 반복 구절을 읽기 전용으로 집계한다.
2. **근거 조사** — 이번 배치 인물의 근거 묶음을 만든다.
3. **판정** — 유지·개선·신규·보류를 결정하고 이유를 남긴다.
4. **작성** — GPT-5.6이 개선 또는 신규 후보를 만든다.
5. **1차 독립 검토** — 새 GPT 컨텍스트가 근거 저항성을 검토한다.
6. **수정** — 작성 컨텍스트와 분리된 새 실행이 blocking·major만 고친다.
7. **2차 통독** — 인물 고유성, 쉬운 한국어, 삶과 철학의 연결을 다시 읽는다.
8. **조건부 게시** — 시작 당시 원문 해시가 현재 DB와 같고 후보 상태가 `approved`일 때만 갱신한다.
9. **재실행 검증** — 같은 배치를 다시 실행하면 전원 `SKIP`이어야 한다.
10. **서비스 검수** — 캐시 갱신 후 변경 인물의 실제 한국어 화면을 전원 확인한다.

## 2026-07-30 처리량 전환

기존 방식은 활성 실존 인물 전원을 dossier 조사와 최대 9회 모델 호출이 필요한
재작성 대상으로 취급했다. 25명 한 배치가 지나치게 오래 걸렸고, 좋은 공개 원고도
불필요하게 다시 쓰는 구조였다. `VM-KO-05` 마감 뒤 다음 두 단계로 분리한다.

### 1. 빠른 전수 판정

- 아직 정식 배치에 없는 활성 `full+light` 실존 인물을 100명씩 고정한다.
- 한 모델 호출에 원고 25명씩 묶고 서로 다른 100명 배치 두 개에 2슬롯씩 나눠
  마지막 느린 호출의 유휴 시간을 줄인다. 전체 모델 동시성은 최대 4다.
- 기존 원고와 구조 신호만 읽어 `keep / improve / hold`로 판정한다.
- 이 단계에서는 dossier·후보·승인·DB 쓰기를 만들지 않는다.
- `keep`은 원고를 그대로 보존하고 `status=reviewed`로 마감한다.
- 근거가 필요한 내면·인과·논쟁적 자기고백은 즉시 `hold`로 마감하며 이유와
  필요한 조사 조건을 남긴다.
- 첫 문장 이름 노출, 짧은 분량, 괄호 수만으로 improve를 만들지 않는다.

### 2. 결함자만 사실 비증가 개선

- `improve`만 여러 명씩 묶어 현재 원고의 사실 재료를 늘리지 않는 편집 후보를
  만든다.
- 새 이름·사건·연대·직책·작품·인과·동기·감정·직접 인용을 보태지 않는다.
- 숫자와 직접 인용은 원문 부분집합인지 기계 가드로 먼저 확인한다.
- 별도 모델 호출의 `fidelity` 렌즈가 원문 대비 사실 비증가를, `editorial`
  렌즈가 인물성·구조·쉬운 한국어를 검토한다.
- 서로 다른 배치 두 개를 병렬 처리하되 배치당 동시 호출을 2로 나눠 전체 상한
  4와 같은 파일 단일 작성자 원칙을 함께 지킨다.
- 두 렌즈를 통과한 후보만 루트 작업자가 본문 전체를 직접 읽고 명시 승인한다.
- 통과하지 못한 후보는 반복 재작성하지 않고 보류해 현재 DB 원문을 보존한다.
- 승인은 자동화하지 않는다. 조건부 DB 게시·캐시·재실행 `SKIP`·공개 HTML
  검증도 기존 출고 가드를 그대로 쓴다.

각 단계는 모델 출력을 임시 파일에 분리하고, 성공분을 배치 JSON에 한 번만 원자
병합한다. 부분 실패 시 완료분은 자동 `SKIP`하고 실패분만 최대 두 차례
재실행한다. 전수 감사는 활성 실존 인물의 누락·중복·DB 해시를 함께 검사한다.

### 결함 등급

- `blocking`: 사실 오류, 오인용, 동명이인, 근거 없는 내면·인과, 원문과 반대 의미, 작업 지시 누출
- `major`: 인물 교체 가능, 삶과 철학 단절, 영웅화·참회 세탁, 설명문·정책문으로 평준화, 핵심 반론 삭제
- `minor`: 번역투, 무생물 주어, 종결 반복, 불필요한 괄호, 문단 호흡, 국소 중복

blocking이 하나라도 남으면 게시하지 않는다. major는 고친 뒤 원고 전체를 다시 읽는다.

## 후보 배치와 게시 가드

정식 배치는 `docs/celeb-data/virtual-monologue/`에 둔다. 바꾼 인물만 이전 본문을 포함해 복구 가능하게 보존한다.

필수 필드:

- `batchId`, `promptVersion`, `model`, `createdAt`
- `slug`, `nickname`, `profileType`
- `route`, `routeReason`
- `currentText`, `currentHash`
- `dossier`, `candidateText`, `candidateHash`, `candidatePromptVersion`
- `reviews[]`: `evidence | editorial` 렌즈, 모델·프롬프트 버전·시각·판정·강점·심각도별 결함
- `review.blocking`, `review.major`, `review.minor`: 두 독립 검토의 게시용 집계
- `approval`: 승인 시각·승인 주체·재독 판단 메모
- `status`: `draft | reviewed | approved | rejected | hold | published`
- `publishedAt`, `liveVerifiedAt`

게시기는 다음 조건을 모두 확인한다.

- `status=approved`
- evidence·editorial 검토가 각각 존재하고 둘 다 `pass`
- 두 검토와 집계에서 blocking·major 0
- 승인 주체·시각·판단 메모가 모두 기록됨
- DB 현재 본문의 SHA-256이 `currentHash`와 일치
- 후보가 비어 있지 않음
- 한자·em dash·URL·마크다운 링크·작업 지시 없음
- 말투 일관성

## 파일럿과 전수 순서

### 24명 파일럿

- 현대 기술·기업·정치 6명
- 역사 지도자·사상가 6명
- 과학자·예술가 6명
- 자료가 얇거나 기존 글이 특히 위험한 인물 6명

유지 후보도 포함해 좋은 글을 망가뜨리지 않는지 반증한다. 영향력이 높거나 판정이 애매한 인물만 개선·신규를 둘 다 만들어 블라인드 비교한다.

### 전수 배치

1. 결측·형식 blocking
2. 영향력·노출도가 높은 실존 인물
3. 논쟁적인 현대 실존 인물
4. 나머지 실존 인물
5. fiction 원전 트랙
6. 한국어 승인분의 영문 후속

파일럿 이후 배치 크기는 25~40명이다. 매 배치마다 유지·개선·신규·보류·게시 수와 실패 명단을 이 문서에 적는다.

## 체크리스트

### Phase 0 — 기준선과 안전장치

- [x] 문제 정의와 GPT 작성 모드 3명 실험
- [x] 전수 정비 SSoT 문서 작성
- [x] 현재 활성 인물·필드 결측 기준선 재측정
- [x] 기존 실존 인물 생성기를 후보 저장 기본으로 변경
- [x] 생성기 무인자 전량 실행 차단 및 DB UPDATE 제거 확인
- [x] 조건부 게시 전에는 후보를 DB에 반영할 수 없는지 게시기까지 확인

### Phase 1 — 구조 감사

- [x] 전수 구조 감사 스크립트 구현
- [x] PostgREST 1,000행 상한을 피한 1,527명 조회 검증
- [x] 판박이 오프닝·결말과 반복 구절 기준선 기록
- [x] 구조 신호를 최종 품질 판정으로 오용하지 않는 보고 형식 확인

### Phase 2 — 작업대

- [x] 근거 묶음·후보·검토 결과 배치 스키마 확정
- [x] 후보 생성기 구현
- [x] evidence·editorial 독립 비판 검토기 구현
- [x] 검토 반영 수정기와 수정 시 이전 검토 무효화 구현
- [x] 생성·검토·수정의 제한 병렬화(`--concurrency 1~4`)와 성공분 단일 원자 병합 구현
- [x] 같은 배치의 생성·검토·최대 2회 수정·재검토를 겹침 없이 잇는 순차 배치 러너 구현
- [x] 기존 배치 중복을 제외하는 다음 배치 읽기 전용 선택기 구현
- [x] 두 검토 통과 뒤 명시적 승인기 구현
- [x] SHA-256 조건부 게시기와 service_role 전용 원자적 RPC 구현
- [x] dry-run → apply → 재실행 `SKIP` 검증
- [x] 공개 가상 독백 탭의 서버 렌더링 문단과 후보 완전 일치 검증기 구현

### Phase 3 — 24명 파일럿

- [x] 명단과 선정 이유 기록
- [x] 24명 근거 묶음 완성 — 24/24
- [x] 유지·개선·신규·보류 판정 — 24/24
- [x] 후보 작성·두 차례 비판 검토 — 17명 통과·7명 보류
- [ ] 게시 전후 DB·캐시·실화면 검증 — 게시 17명 DB·캐시·공개 HTML 완료, 연결 가능한 시각 브라우저가 없어 육안 검수 대기
- [x] 첫 6명에서 드러난 규격 결함을 evidence v2·editorial v3에 반영

### Phase 4 — 전수 한국어

- [x] 활성 실존 `full`·`light` 1,476명 전수 판정
- [x] 게시 139명 공개 SSR HTML 문단 완전 일치
- [x] 보류 713명 사유와 재개 조건 기록
- [x] 누락·중복·해시·상태 최종 완료 감사

### Phase 5 — 영문

- [ ] 한국어 승인분만 영문 후보 생성
- [ ] 문장·문단·순서 1:1 대응 금지 원칙 검증
- [ ] 변경 인물 영문 실화면 전수 확인

## 배치 기록

| 배치 | 범위 | 상태 | 유지 | 개선 | 신규 | 보류 | 게시 | 비고 |
|---|---|---|---:|---:|---:|---:|---:|---|
| VM-A0 | 착수 당시 구조 감사 | 완료 | — | — | — | — | 0 | blocking 4, high 6. 자동 품질 판정 아님 |
| VM-P0 | 피터 틸·알렉스 카프·마크 안드레센 | 실험 완료 | 0 | 2 | 1 | 0 | 0 | GPT 개선/신규 블라인드 비교. 후보는 게시하지 않음 |
| VM-P1 | 24명 파일럿 | 완료 | 0 | 17 | 0 | 7 | 17 | 첫 두 묶음은 게시 9·보류 3. 마지막 묶음은 왕양명·소크라테스·공자·마리 퀴리·반 고흐·프리다 칼로·베토벤·세종 게시, 광해군·아인슈타인·큐브릭·푸틴 보류. 게시 전원 조건부 UPDATE·캐시 HTTP 200·재실행 `SKIP`·공개 HTML 일치 |
| VM-KO-01 | 구조 medium·영향력 상위 실존 25명 | 완료 | 0 | 15 | 1 | 9 | 16 | 최초 후보 동시 통과 0명 → 1차 수정 뒤 10명 → 2차 수정 뒤 13명. 사실 비증가 국소 인간 편집 4명 중 3명 추가 통과·프랭클린 보류. 게시 16명 조건부 UPDATE·캐시 HTTP 200·재실행 `SKIP`·공개 HTML 일치 |
| VM-KO-02 | 구조 medium·영향력 차상위 실존 25명 | 완료 | 0 | 14 | 1 | 10 | 15 | 두 검토·직접 통독 뒤 게시 15명. 조건부 UPDATE·캐시 HTTP 200·재실행 `SKIP`·공개 HTML 일치. 보류 10명은 후보·검토 이력과 사람 재구성 조건을 보존 |
| VM-KO-03 | 구조 medium·영향력 후속 실존 25명 | 완료 | 0 | 10 | 2 | 13 | 12 | 두 검토·직접 통독 뒤 게시 12명. 조건부 UPDATE·캐시 HTTP 200·재실행 `SKIP`·공개 HTML 일치. 라파엘로 포함 보류 13명은 후보·검토 이력과 사람 재구성 조건을 보존 |
| VM-KO-04 | 구조 medium·영향력 후속 실존 25명 | 완료 | 0 | 13 | 1 | 11 | 14 | 최초 통과 3명 → 1차 수정 뒤 10명 → 2차 수정 뒤 13명. 사람 국소 교정 7명 중 3명 추가 통과. 게시 14명 조건부 UPDATE·캐시 HTTP 200·재실행 `SKIP`·공개 HTML 일치 |
| VM-KO-05 | 구조 medium·영향력 후속 실존 25명 | 완료 | 0 | 7 | 2 | 16 | 9 | 정밀 dossier 25/25. 두 독립 검토·사람 통독 뒤 9명 게시, 16명은 기존 DB 원문 보존 |
| VM-KO-06 | 정밀 조사 9명 + 빠른 판정 16명 | 완료 | 6 | 1 | 0 | 18 | 1 | 아담 스미스 1명 게시. 개선 불합격 8명과 빠른 보류 10명은 현재 원문 보존 |
| VM-KO-T-01~14 | 미포함 활성 실존 1,303명 빠른 전수 | 완료 | 618 | 55 | 0 | 630 | 55 | 개선 필요자만 사실 비증가 재작성·두 독립 검토. 모델 통과 뒤 사람 재독에서 14명 추가 보류. 게시 전원 조건부 UPDATE·캐시 HTTP 200·재실행 `SKIP`·공개 HTML 일치 |

## 첫 6명에서 확정한 운영 교훈

- 외부 비판·위험을 화자의 후회·약속·자기비판으로 바꾸지 않는다. 직접 근거가 없으면 검증된 사실을 병치한다.
- evidence는 사실·인용·내면·용어 정확성, editorial은 목소리·구조·쉬운 한국어만 책임진다. editorial이 새 사실을 요구하거나 사실 지원 여부를 판정하지 않는다.
- 낯선 개념의 짧은 괄호 안내는 서비스 필수 장치다. 괄호가 있다는 이유만으로 결함으로 잡지 않는다.
- `keep`도 긍정적으로 입증해야 한다. 젠슨 황의 최초 유지 판정은 근거 검토에서 무너져 개선으로 바뀌었다.
- `논쟁을 숨기지 말라`는 지시만 강하게 주면 모델이 모범적인 자기반성을 창작한다. 사건·결과와 실제 입장을 나란히 두고 해설자의 윤리 결말을 붙이지 않는다.
- 한 인물의 반복 실패로 전체 검수 규격을 계속 바꾸지 않는다. 후보 수정 2회 뒤에도 major가 남으면 기존 서비스 글을 유지하고 보류한다.
- 알렉스 카프는 WEF 장문 대담까지 근거를 확장했지만 회사 정책문 구조가 반복됐다. 비회사 장문 대담의 추가 근거 또는 인간 편집자의 새 중심축 결정이 재개 조건이다.
- 게시 5명은 조건부 DB UPDATE, `celebs` 캐시 갱신 HTTP 200, 게시기 재실행 `SKIP`, 공개 한국어 탭의 승인 문단 완전 일치를 확인했다. 연결 가능한 시각 브라우저가 없어 `liveVerifiedAt`은 아직 채우지 않았고 `liveHtmlVerification`만 기록했다.

## 둘째 6명에서 확인한 운영 교훈

- 링크 혼입처럼 구조상 명백한 결함도 단순 삭제로 끝내지 않는다. 히파티아·손빈은 링크 주변에 근거 밖 감정과 편집자식 사료 해설이 함께 있어 한 번의 비판 수정이 필요했다.
- 근거 묶음의 `researchLimits`는 실제 검토에서 작동했다. 히파티아의 기구 수령, 손빈의 감정 배제, 베버의 평생 문제의식, 밀의 어린 시절과 자유론 사이 직접 인과가 차단됐다.
- 두 차례 수정 뒤에도 major가 남은 페리클레스는 숫자를 채우기 위해 게시하지 않았다. 재개 조건은 배심 수당·시민권법·전쟁 전략만 남기는 인간 편집 구조의 선확정이다.
- relation 티어는 웹 코드에서 독백을 의도적으로 준비되지 않은 항목으로 처리한다. 클로윈디의 빈 값은 공개 결측이 아니며, 익명 계정의 얇은 생애 자료를 800자 자기서사로 부풀리지 않는다.
- 둘째 묶음 게시 4명도 조건부 DB UPDATE, 캐시 갱신 HTTP 200, 재실행 전원 `SKIP`, 공개 한국어 HTML 승인 문단 완전 일치를 확인했다. 누적 게시 검증은 9명이다.

## 마지막 12명에서 확인한 운영 교훈

- 제한 병렬화는 모델 호출만 동시에 실행하고 결과를 한 번에 원자 병합해야 안전하다. 같은 배치 파일을 쓰는 생성·검토 CLI 두 프로세스를 동시에 실행하지 않는다.
- 근거 검토와 편집 검토는 실제로 서로 다른 결함을 잡았다. 공자·광해군·푸틴에서는 외부 비판을 자기반성으로 옮기는 문제를, 반 고흐·프리다·베토벤·큐브릭에서는 `A가 아니라 B`식 교정 문장 반복을 별도로 걸렀다.
- 어려운 정보의 괄호 안내는 개념어뿐 아니라 독자가 맥락 없이는 알기 힘든 기관·제도·작품 형식에도 필요하다. 다만 모든 고유명사에 괄호를 붙이지 않고 이해를 막는 첫 등장에만 둔다.
- 광해군은 정치적 책임을 수동태로 세탁하지 않으면서도 후대 편집자의 모범적 자백을 입히지 않는 사실 귀속 문장이 필요하다. 해당 문장 구조를 인간 편집으로 먼저 확정하는 것이 재개 조건이다.
- 아인슈타인은 자유 낙하 사고실험과 일반 상대성 이론 구상의 직접 근거가 dossier에 부족했다. 근거를 보강하거나 그 인과를 제거하기 전에는 게시하지 않는다.
- 큐브릭은 사실 검토를 통과했지만 교정형 대조 리듬이 두 차례 수정 뒤에도 남았다. 사실이 맞는 글과 인물 목소리가 살아 있는 글은 별도 게이트다.
- 푸틴은 단일 1인칭 필드의 구조적 한계를 드러냈다. 당사자의 전쟁 정당화만 두면 책임 세탁이 되고, 유엔·국제형사재판소 판단을 그의 입으로 말하게 하면 창작된 자기비판이 된다. 비1인칭 편집 맥락을 분리할 UI·데이터 규칙이 생기기 전까지 보류한다.
- 마지막 묶음 게시 8명은 조건부 DB UPDATE, 캐시 갱신 HTTP 200, 재실행 전원 `SKIP`, 공개 한국어 HTML 승인 문단 완전 일치를 확인했다. VM-P1 누적 게시 검증은 17명이다.

## VM-KO-01 25명에서 확인한 운영 교훈

- 최초 후보는 두 검토 동시 통과가 0명이었다. 1차 수정 뒤 10명, 잔여자 2차 수정 뒤 13명이 통과했다. 생성 한 번이나 고정 체크리스트 한 번으로 품질을 판정하지 않는 이유가 실제 수치로 확인됐다.
- 최초 생성기는 dossier 밖의 널리 알려진 세부를 상식처럼 보태거나 `researchLimits`를 인물의 자기해설로 옮겼다. 생성·수정 프롬프트 v3부터 dossier만 사실 원천으로 쓰고, 현재 서비스 글은 문장 재료일 뿐 사실 근거가 아니며, 조사 한계는 화자가 말할 내용이 아니라 금지선이라고 명시했다.
- 모델 수정 2회 뒤 네 명은 검토자가 정확히 지목한 괄호 형식·출처 귀속·편집자식 결말 삭제만 국소 교정했다. 샤를마뉴·앨런 튜링·링컨은 두 검토를 다시 통과했고, 프랭클린은 구조 major가 남아 보류했다. 이 예외는 새 사실·주장·감정·구조를 더하지 않을 때만 한 번 허용한다.
- 최종 결과는 `published 16 / hold 9`다. 보류 9명은 단순 실패 목록이 아니라 반복 결함과 재개 조건을 각 dossier에 기록했고 기존 DB 원고를 보존했다.
- 순차 배치 러너는 생성 → evidence → editorial → 최대 2회 수정·재검토까지만 자동화한다. 승인·인간 국소 교정·보류·DB 게시를 자동화하지 않은 경계가 실제 출고에서 유효했다.
- 게시 16명은 직접 통독, 명시 승인, 원문 SHA-256 조건부 UPDATE, 캐시 HTTP 200, 같은 게시 명령 재실행 `SKIP`, 공개 SSR 문단 완전 일치를 모두 통과했다. 연결 가능한 브라우저가 0개였으므로 HTML 일치와 CSS·탭·스크롤 육안 검수를 같은 것으로 기록하지 않았고 `liveVerifiedAt`은 비워 뒀다.
- 후속 배치에서는 두 안전 가드가 즉시 결함을 잡았다. 뉴턴 dossier의 등록되지 않은 URL은 병합을 차단했고, 노자의 `제 이름`은 정당한 1인칭인데도 검증식이 놓쳤다. 전자는 근거를 줄여 고쳤고 후자는 생성·게시 양쪽 검증식을 함께 고쳤다.

## VM-KO-04 25명에서 확인한 운영 교훈

- 최초 두 렌즈 통과는 3명이었고, 두 차례 수정 뒤 13명으로 늘었다. 사람 국소 교정은 검토자가 정확히 지목한 용어·귀속·결말만 고친 7명에게 한 번 적용했고, 두 렌즈 재검토에서 3명만 추가 통과했다.
- 국소 교정도 성공을 보장하지 않았다. 러셀은 냉전 초기 강경 핵정책 검토와 우생학·인종 위계의 구조적 연결이 남았고, 주원장·진독수·묵자도 설명문 구조와 귀속 문제가 남아 즉시 보류했다.
- 보류를 수작업 JSON 수정으로 마감하지 않도록 당시 전용 처리기를 사용했다. 후보 해시·두 검토·잔여 blocking/major를 검증하고 최신 후보·검토를 `reviewHistory`로 옮긴 뒤 기존 DB 본문을 보존했다. 완료 후 이 일회성 처리기는 사용자 요청에 따라 제거했다.
- 게시 14명은 루트 작업자의 전원 통독, 명시 승인, 원문 SHA-256 조건부 UPDATE, 캐시 HTTP 200, 게시기 재실행 전원 `SKIP`, 공개 SSR 문단 완전 일치를 통과했다.
- 연결 가능한 브라우저가 0개여서 CSS·탭·스크롤·모바일 육안 검수는 하지 못했다. `liveVerifiedAt`은 비워 두고 HTML 검증만 기록했다.

## 실존 한국어 완료 판정

- 활성 인물 전원이 `유지 / 개선 / 신규 / 보류` 중 하나로 근거와 함께 판정됨
- 정밀 dossier 트랙의 유지 인물은 근거 대조 완료
- 빠른 전수 판정의 유지 인물은 텍스트 편집 판정임이 기록되고 DB를 바꾸지 않음
- 게시분 blocking 0
- 게시 신규·개선분 모두 독립 비판 검토와 사람 재독을 통과함
- 정밀 트랙의 원문·후보는 기존 tracked 배치에 보존됨. 빠른 트랙의 개인별 중간 기록은 완료 후 사용자 요청으로 제거됨
- 조건부 게시기 재실행 결과 변경 예정 0건
- 변경 인물 한국어 실화면 전수 검증
- 정밀 보류 인물은 부족한 자료와 재개 조건이 기존 배치에 남음. 빠른 보류 인물은 재개 시 DB 원문에서 새로 판정함

`fiction` 원전 트랙과 영문 후속 검수는 이 실존 한국어 완료 범위에 포함하지 않는다.
