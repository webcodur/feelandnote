# Windows reserved names
nul

# Environment variables
.env
.env.*
!.env.example

# MCP 설정 — access token이 평문으로 들어가므로 커밋 금지 (노출 시 토큰 자동 폐기)
.mcp.json

# Google credentials (GA, YouTube OAuth)
**/credentials/

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
out/
dist-ssr
*.local
.next
**/.next-*/

# Build & language caches (자동 재생성)
*.tsbuildinfo
__pycache__/
*.pyc

# Remotion auto-generated docs (DB에서 재생성 가능)
docs/project/remotion/book-recommend/lineup/candidates-raw.md

# Remotion episodes — 전체 로컬 관리
sw/remotion/public/episodes/
sw/remotion/episodes/

# Remotion old voice directories (episodes/로 이전됨)
sw/remotion/public/voice/
sw/remotion/public/voice-profile/
sw/remotion/public/voice-samples/

# Remotion cover images (candidate용, DB thumbnail_url에서 재다운로드 가능)
sw/remotion/public/covers/

# Remotion 세력도(faction) 영상 자료 — 활성 여부와 무관하게 한 뿌리에서 전체 로컬 관리
sw/remotion/public/factions/

# 렌더 창고 — 렌더 직전에 그 편 자산만 하드링크로 모아 두는 임시 폴더(렌더가 끝나면 지운다).
sw/remotion/.render-stage/

# Remotion BGM 음원 (로컬 관리)
sw/remotion/public/music/

# Remotion book-person·rankings 시리즈 자료 — 로컬 관리
sw/remotion/public/book-person/
sw/remotion/public/rankings/

# 담화 내보내기 백업 — DB 발효 때마다 쌓이는 회차 사본이라 추적하지 않는다.
# (담화 데이터 세 파일 자체는 추적 대상이다. factions/ 처럼 통째로 빼지 않는다)
sw/remotion/public/discourses/*/.export-backup/

# Remotion temp scripts & data
sw/remotion/tmp-*
sw/remotion/subs-*.json
sw/remotion/subs.json
sw/remotion/vg-story.md
sw/remotion/_scan_shorts.mjs
sw/remotion/fix-json.cjs
sw/remotion/scripts/voice/_test-*.ts
sw/remotion/scripts/voice/test-voices.ts
sw/remotion/scripts/voice/subs-*.json
sw/remotion/scripts/_cleanup-active-log.txt
sw/remotion/scripts/_audit-active-out.json
sw/remotion/.old_*
sw/remotion/.render-check/
sw/remotion/_dbg_*

# sw/ 작업 메모 (로컬 보관용)
sw/인물-*.txt

# Root-level temp scripts
/cp-*.cjs
/copy-*.cjs
/copy_images.js
/copy-*.ps1
/map-*.cjs
/scan-*.cjs
/survey*.cjs
/_fix-subs.mjs
/virtual_monologue_test.md
/.tmp_*
/tmp_*
/clean-symlinks.ps1
/scripts/update-*.js
/anthropic_script_dump.*

# Root-level MCP 툴 스키마 덤프 & 산출물 (로컬 재생성)
/mcps/
/output/

# 작업 메모 (로컬 보관용 — docs/resource/ 로 이동, 루트 패턴은 잔존 파일 대비 유지)
/CELEB_QUEUE.md
/SHORTS_EXAMPLE.txt
/최고 쇼츠.txt
/프롬프트_*.txt
/all-refs.txt
/plan-draft.md
/이미지-재료쓰기.md
/정복군주.txt
/보이스 에디터.txt
/조사요구서-*.md
/이미지분석.txt
/살아있는 프로젝트.txt
/docs/resource/프롬프트_*.txt
/docs/resource/보이스 에디터.txt
/docs/resource/내 할 일.txt

# 개인 운영 자료실 — AI 구독 상태와 결제 메모는 로컬 전용
/private/ai-subscriptions/

# 일회성 작업 스크립트·중간 산출물 (파일명 앞에 .tmp- 를 붙인다)
.tmp-*
**/.tmp/
/sw/web-bo/scripts/_targets.json

# 감사·점검 리포트 (로컬 재생성)
/.artifacts/

# 자산 백업 (git 추적 밖 자산의 원본 보관 — 절대 지우지 않는다)
/_backup-*
/_backup/

# web-bo 이미지 생성·조사 산출물 (원본은 로컬 보존, 서비스용 파일만 별도 경로에서 추적)
/sw/web-bo/output/

# 로컬 조사 세션·다운로드 임시파일
/sw/web-bo/.kci_cookies.txt
/sw/web-bo/*.ru-orig.vtt
/sw/web-bo/scripts/_debug-*

# Supabase CLI가 현재 연결 상태를 기록하는 로컬 캐시
**/supabase/.temp/

# 외부 AI 에이전트 설정
# .agents = 멀티툴 공용 스킬·문서 (git 추적)
# 아래는 .agents/skills 를 가리키는 로컬 정션이다. 실체가 아니므로 추적하지 않는다.
# 새 컴퓨터나 새 스킬 추가 시 `.agents/link-skills.ps1` 로 다시 만든다.
/.claude/skills/audit-web-i18n/
/.claude/skills/celeb-activation-audit/
/.claude/skills/celeb-reading/
/.claude/skills/diagnosing-bugs/
/.claude/skills/faction-character-info/
/.claude/skills/faction-data-manage/
/.claude/skills/faction-dialogue-review/
/.claude/skills/fiction-profile-audit/
/.claude/skills/interview-audio-pipeline/
/.claude/skills/nobg-cutout/
/.claude/skills/remo-resource-scanner/
/.claude/skills/supabase/
/.claude/skills/supabase-postgres-best-practices/
/.claude/skills/ui-scroll/
/.claude/skills/win-lock-bypass-editor/
/.claude/skills/writing-for-agents/
/.codex/
/scratch/
/_workspace/
/sw/scripts/
/scripts/remotion/

# 스케줄러·기타 캐시
.claude/scheduled_tasks.lock

# Wrangler cache
.wrangler/

# Backups & temp
backups/
tmp/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Remotion 번들 캐시 (고정 outDir)
.cache/

# Claude Code 개인 설정 (로컬 전용 — 키·경로가 섞이므로 추적하지 않는다)
.claude/settings.local.json

# 다른 AI CLI의 로컬 설정 (command-code 등)
.commandcode/

# 조사·적재 작업 중 만드는 임시 산출물. 쓰고 나면 지운다
_tmp-*
**/_tmp-*

# 릴레이 스냅샷은 스크립트로 다시 뽑는다. 회차마다 7MB씩 쌓이므로 추적하지 않는다
data/celeb/reading-relay/all-readings.json

# 회차 작업 폴더(묶음·레인 결과). 반영이 끝나면 원장에만 남는다
data/celeb/reading-relay/wave/
data/celeb/headline-rewrite/packs/
data/celeb/headline-rewrite/drafts/

# msys/git-bash 크래시 덤프
*.stackdump
tmp-*
