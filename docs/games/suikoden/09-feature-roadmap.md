# 09. 기능 로드맵

> **최종 실측 체크: 26.07.30** — 천도 코드·검증 결과와 완료/잔여 항목 대조

> 기획서(01~08) 대비 현재 구현 상태 분석 및 우선순위별 구현 계획.
> 파일·상수 단위의 실측 근거는 `10-implementation-status.md`에 있다.

---

## 현재 구현 상태 (2026-07-30 코드 실측)

### 완료

| 시스템 | 구현 내용 |
|--------|----------|
| **공개 진입** | 쉼터 천도 카드 또는 `/[locale]/rest#suikoden` 직접 접근으로 게임 개방. 비밀코드 제거 |
| **턴제 엔진** | `advanceTurn()` — 시간 진행(+10일/턴), 건설/자원/급여/식량/훈련/체력/민심/인구 처리 |
| **2단계 셋업** | 시나리오 5종 중 택1 → `previewScenario()` → AI 세력 미리보기 + 시나리오 지정 후보 중 주군 선택 → `finalizeGame()` |
| **시나리오 인물 검증** | 고정 ID만 DB 조회. 선택 시나리오 필수 인물이 빠지면 시작 불가와 누락 안내 |
| **시나리오** | 삼국쟁패/십자군/전국시대/나폴레옹/대몽골 5종, 시나리오별 활성 거점·지역 제한 |
| **페르소나 Grade** | `celeb_persona` 4능력 가중평균 우선, 없으면 `total_score` 폴백 |
| **방랑·거병** | 지역 이동, 동료 모집(최대 5명), 랜덤 이벤트, 빈 영토 거병 |
| **건설·생산** | 15종 건물, 건설→시공→완성, 담당관 배치, 자원·무기·예비 병사 생산 |
| **병사 보충** | 병영이 만든 세력 예비 병사를 선택 인물의 병력 상한까지 배치. 플레이어와 AI 동일 규칙 |
| **식량·근무·순찰** | 병력 기반 식량 소모, 건물 근무 배치, 위협 출몰·배정·해결 |
| **인재·인사** | 선술집 등용, 자동 내정, 훈련, 포상/처벌, 체력 소모·회복 |
| **전투** | 3×5 개별 인물 턴제. 실제 주둔 인물 출진, 14기술 대상 선택, 함정·탐지·일기토·상태효과, 최대 30턴 |
| **전투 손실 연속성** | 기존 부상·병력을 전투 시작에 반영하고 종료 후 체력·병력 손실을 캠페인에 유지 |
| **점령·포로** | 인접 영토 침공, 무주지 무혈 점령, 패배측 주둔 인물 후퇴, 등용/석방/처형 |
| **AI** | 인접 거점의 실제 주둔 인물만 동원하고 출진 전 업무 해제. 내정·전투·침공·보충 수행 |
| **캠페인 종료** | 플레이어 영토 0 패망, 활성 영토 전부 점령 통일, 난이도 제한 턴 초과. 빈 AI 세력 정리 |
| **자동 저장·이어하기** | 브라우저 단일 자동 저장. 저장 실패 경고, 결과 확정 시 저장본 삭제, 저장된 AI 전투도 자동 진행 |
| **세계맵·UI** | 10지역 22영토, 지구본·텍스트맵, 주요 화면 모바일 세로 스크롤·320px 대응 |
| **장비** | 수량제 4종(무기/군마/조선/부적), 배분·AI 자동 분배·전투 보정 |
| **외교·경영** | 동맹/정전/조공/항복 권유, 세율, 철거, 민심, 인구 변동 |
| **이벤트·명성·급여** | 계절/방랑/위협 이벤트, 명성 0~1000, 등급별 급여 |
| **대사·i18n·오디오** | DB 대사 + 기본 대사, ko/en UI·로그 후번역, 페이즈별 BGM 2트랙 |

### 미구현·후속

| 카테고리 | 남은 항목 | 근거 |
|----------|----------|------|
| **인사 확장** | 학당 학습(intellect/virtue 성장) | `academy`의 `special:'discover'`를 읽는 코드 없음 |
| **외교 확장** | 이간(두 AI 세력 관계 악화) | 해당 커맨드 없음 |
| **경영 확장** | 인구→병력 징병, 민심 세분화(폭동/반란) | 예비 병사 배치는 구현됐지만 인구 전환은 없음 |
| **승리 조건 확장** | 문화 승리, 외교 승리 | 현행 정식 승리는 활성 영토 통일 |
| **장비 확장** | 구매 UI, 수상전(조선) 보정 | `EQUIPMENT_COST` 상수 없음, `ships` 전투 미반영 |
| **이벤트 확장** | 캐릭터 조합 이벤트, 이벤트 팝업 UI | 팝업 컴포넌트 없음, 로그만 |
| **저장 확장** | 수동 저장·여러 슬롯·서버 동기화 | 현행은 브라우저 자동 저장 1개 |
| **라우트 잔재** | `navigation.tsx`의 `/rest/suikoden` 링크 | 전용 `page.tsx`가 없어 404. 실제 진입은 `/rest#suikoden` |
| **에셋** | 거점 배경 3장, 지역 배경 2장, 효과음 | 파일 없음 |

---

## 구현 우선순위 (잔여)

### Phase 0: 정합성 복구

| 항목 | 설명 |
|------|------|
| **죽은 링크** | `navigation.tsx`의 `/rest/suikoden`이 404. 페이지 신설 또는 `/rest#suikoden`으로 교정 |
| **누락 에셋** | 거점 배경 3장 추가, 또는 배경 URL을 `imageUrl` 필드 기반으로 교정 |
| **죽은 코드** | 미사용 전술 상수(`TACTIC_MATCHUP`/`TACTIC_INFO`/`CLASS_TACTIC_BONUS`), 미호출 `previewWorld`/`initGame` 처리 방침 결정 |

### Phase A: 이벤트 팝업 + UI 개선

| 항목 | 설명 |
|------|------|
| **이벤트 팝업** | 이벤트 발생 시 화면 중앙 팝업, 확인 버튼 |
| **영토 비교** | 세력별 영토/자원/병력 비교 테이블 |

### Phase B: 인사/경영 확장

| 항목 | 설명 |
|------|------|
| **학당 학습** | 학당에 배치 → intellect/virtue 성장 |
| **징병** | 병영에서 인구→병력 전환, 민심 -5 |
| **민심 세분화** | 폭동(0-19) 시 건물 파괴/인재 이탈 이벤트 |

### Phase C: 승리 조건 확장

| 항목 | 설명 |
|------|------|
| **문화 승리** | 극장/사원 문화 영향력 → 전체 60% 이상 |
| **외교 승리** | 2/3 이상 세력과 동맹 체결 |

### Phase D: 장비 + 저장 확장

| 항목 | 설명 |
|------|------|
| **장비 구매 UI** | 비용 상수 정의 후 무기고에서 금/자재로 구매 |
| **수상전** | `ships` 전투 보정 설계·구현 |
| **저장 확장** | 현행 자동 저장 1개에 수동 저장·여러 슬롯·서버 동기화가 필요할지 판단 |

---

## 현재 검증 상태 (26.07.30)

- 천도 범위 ESLint: **0 errors / 19 warnings**
- 천도 관련 경로 `git diff --check`: **통과**
- Node 22 내장 TypeScript 로딩으로 시나리오 5종 확인
- 전체 TypeScript 검사와 `pnpm build:web`은 천도 밖의 사용자 수정 파일이 존재하지 않는 `scriptures/ko/ai-academy.json`, `scriptures/en/ai-academy.json`을 가져와 중단됐다. 이 두 건 외 천도 타입 오류는 보고되지 않았다.
- 브라우저 실제 한 판 완주, 한국어/영어·모바일 시각 확인, 실 DB 고정 UUID 전원 생존 여부는 미검증이다.

따라서 핵심 완주 흐름은 코드상 연결됐지만, 위 검증이 끝나기 전에는 배포 가능 확정으로 표시하지 않는다.
