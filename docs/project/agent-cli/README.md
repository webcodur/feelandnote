# 외부 CLI 작업 기법

외부 CLI 모델을 한 작업 안에서 조사·작성·검수에 어떻게 배치할지 기록한다. CLI별 실행 파일, 모델 선택, 로그인, 타임아웃, 동시성, 결과 수신 규격은 각 스킬이 쥐며 여기에는 복제하지 않는다. 도메인별 작성 규칙과 DB 반영 명령도 해당 도메인 문서가 쥔다.

## 기법

| 기법 | 사용할 때 |
|---|---|
| [`진단-편집 분업`](diagnose-edit-verify.md) | 기존 자료를 넓게 감사하되 강한 모델의 작성·검수 비용은 문제 후보에만 쓸 때 |
