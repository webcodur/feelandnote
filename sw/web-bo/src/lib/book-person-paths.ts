import { BOOK_PERSON_DIR } from '@feelandnote/shared/bo/episode-store'

export { BOOK_PERSON_DIR }

/** 공용 사진 부품이 `/api/{시리즈}/media` 를 부르므로 이 값을 주소 첫 토막에 둔다 */
export const BOOK_PERSON_SERIES = 'book-person'
