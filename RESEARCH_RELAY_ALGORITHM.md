# 독립 조사 릴레이 알고리즘

서로 독립된 대상을 반복 조사해 최종 데이터를 채우는 작업은 이 문서만 따른다.

## 데이터 원칙

- 작업 단위는 인물, 콘텐츠, 장소처럼 서로 독립적으로 완료할 수 있는 대상 하나다.
- 대상은 최종 데이터가 없거나 작업 기준상 미완성인 항목에서 고른다.
- 영속하는 것은 서비스가 사용하는 최종 도메인 데이터뿐이다.
- 배정, 후보, 진행 상태, 재시도, 검수 인계, 실행 로그는 현재 세션에서만 관리한다.

## 오케스트레이터

1. 작업 가능한 대상 목록을 배정 목적으로만 조회한다.
2. 빈 조사 레인마다 서로 겹치지 않는 대상 하나를 배정한다.
3. 조사자의 `완료` 또는 `실패` 보고만 받는다.
4. 완료된 레인에는 다음 대상을 배정한다.

조사자의 `완료` 보고가 해당 작업의 종료 조건이다. 오케스트레이터의 책임은 배정과 릴레이이며,
완료된 결과의 내용 검사나 승인까지 확장되지 않는다.

릴레이 전체가 끝난 뒤에는 개별 결과를 재승인하지 않고, 중복·필수값 누락·티어 모순·깨진 문자만
읽기 전용으로 일괄 검사할 수 있다. 발견된 결함은 담당 데이터 규칙에 따라 사실관계만 교정한다.

## 조사자

조사자는 배정받은 대상 하나만 다음 순서로 처리한다.

1. 대상의 신원과 작업 필요 상태를 확인한다.
2. 신뢰할 수 있는 출처로 조사한다.
3. 조사 결과를 지정된 최종 데이터 저장소에 반영한다.
4. 같은 조사자가 저장된 자기 결과를 한 번 자체검증한다.
5. 발견한 이상은 같은 자체검증 단계 안에서 자기 결과만 바로 교정한다.
6. 대상 식별자, 최종 결과 수, `완료` 또는 `실패`만 보고하고 종료한다.

자체검증 항목은 작업 대상에 맞게 적용한다.

- 잘못된 대상 연결 또는 동명이인
- 사실, 연대, 순서, 논리의 모순
- 중복 결과
- 출처와 결과의 불일치
- 언어별 의미 불일치
- 근거 없는 메타데이터
- 누락, 깨진 문자, 저장 실패

완료할 수 없으면 자신이 추가한 미완성 결과를 남기지 않고 실패 사유만 현재 세션에 보고한다.

## 레인 경계

- 각 조사 레인은 자기 대상의 조사, 저장, 자체검증까지 소유한다.
- 조사자의 자체검증이 끝나면 해당 대상은 끝이다.
- 오케스트레이터는 완료 보고를 그대로 받아 다음 배정으로 넘어간다.
- 조사자는 다른 레인의 결과에 관여하지 않는다.
- 검증 전용 에이전트, 교차검증, 오케스트레이터 재검증, 승인 단계는 두지 않는다.
- 조사 자원 관리용 테이블, 큐, RPC, 상태 파일은 만들지 않는다.
