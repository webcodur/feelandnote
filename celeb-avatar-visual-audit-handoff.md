# 셀럽 아바타 육안검사 재생성 — 인수인계

> 작성: 2026-07-31
> 범위: 저장소 루트 `육안검사.txt` 에 사용자가 적은 결함 60명의 처리
> 관계: 전수 정비 본류는 `todo_celeb_avatar_overhaul.md` 다. 이 문서는 그 하위의 한 회차 기록이며,
> **절대 규칙과 인물 한 명 처리 절차는 그 문서 §4·§5가 여전히 SSoT** 다. 여기 적은 것은
> 이번 회차에서 새로 확인한 함정과 남은 일이다.

---

## 1. 결과 요약

| 구분 | 인원 | 상태 |
|------|------|------|
| 재생성·서비스 반영 완료 | 57 | R2 역검증 해시 일치·DB 갱신·캐시 무효화까지 확인 |
| 진행 중 | 2 | 톰 행크스(신원 오류), 완적(재작업) — 아래 §5 |
| 사용자 검토 대기 | 2 | 해서, 공융 — 사용자가 직접 판단하겠다고 보류 |

`육안검사.txt` 60명 중 톰 행크스·해서·공융을 뺀 57명을 재생성해 전원 반영했다.
톰 행크스는 이후 사용자 지시로 재작업에 들어갔다.

### 반영 완료 57명

`andrew-feldman` `andrew-ng` `beyonce` `billie-eilish` `bobby-fischer` `cai-yong` `carl-lewis`
`cheng-hao` `cher` `daft-punk` `daosheng` `dong-zhongshu` `edvard-munch` `edward-norton`
`emperor-yang-of-sui` `fei-fei-li` `fernando-pessoa` `han-xin` `hayao-miyazaki` `ichiro-suzuki`
`jeff-dean` `joan-of-arc` `john-hering` `johnny-depp` `khabib-nurmagomedov` `kim-beom-su`
`kim-so-wol` `kim-yuna` `lee-tae-jun` `lee-young-pyo` `li-si` `liang-qichao` `masashi-kishimoto`
`meng-haoran` `muhammad-ali` `naoki-urasawa` `neal-stephenson` `nick-bostrom` `octavio-paz`
`park-sang-young` `paul-valery` `prince-shotoku` `roger-federer` `ronaldo-nazario` `ruan-ji`
`safra-catz` `sigmund-freud` `son-heung-min` `takehiko-inoue` `tang-wei` `walt-disney` `yan-hui`
`yan-ying` `yongle-emperor` `yun-bong-gil` `zhang-yiming` `zigong`

---

## 2. 새로 만든 도구

### `sw/web-bo/scripts/regenerate-celeb-avatars.mjs`

아바타 재생성 전용 러너다. 생성 단계까지만 책임지고 배경 제거·업로드는 하지 않는다
(육안 검수를 건너뛰고 서비스에 나가는 것을 막기 위해 일부러 끊었다).

```bash
cd sw/web-bo
node scripts/regenerate-celeb-avatars.mjs <배치.json> [--limit N] [--concurrency 1] [--force]
```

배치 JSON 한 항목:

```json
{ "slug": "...", "celeb_id": "...", "nickname": "...", "gender": true,
  "ref": "(생략 시 DB의 avatar_url. URL 또는 로컬 절대경로)",
  "note": "인물별 연출 지시(영문). 조명·표정·복식" }
```

- 산출물은 `.tmp/avatar-regen/<slug>.png` (1024 정사각).
- 이미 산출물이 있으면 건너뛴다. 한도에 막혀 중단돼도 같은 명령을 다시 부르면 남은 것만 처리한다.
- 진위검사를 내장했다 — 축소 지문(64×64 md5)으로 참조 원본이 그대로 돌아온 경우와 해상도 미달을 거른다.
- 발주서에 아바타 구도 규격(정면·머리 전체·양쪽 상부 어깨·닫힌 목선)과 사진 질감 강제 문구를 박아 뒀다.
  참조가 회화·흑백이어도 컬러 실사로 나온다.

**동시 실행 주의**: 여러 작업자가 같이 돌릴 수 있으므로 `--concurrency 1` 로 쓴다.
작업 폴더를 통째로 지우던 코드는 남의 작업을 날려서 제거했다.

---

## 3. 이번 회차에서 확인한 함정

### 3.1 OpenAI 이미지 생성은 유명인 얼굴을 거부한다 — 그런데 조용히 실패한다

codex `image_gen` 이 유명인 요청을 안전 필터로 거부한다(HTTP 400 "rejected by the safety system").
월트 디즈니는 네 번 전부, 무하마드 알리·조니 뎁·다프트 펑크·빌리 아일리시도 걸렸다.

🔴 **거부돼도 예외가 나지 않는다.** 세션 로그에서 산출물을 회수할 때 **넣어준 참조 이미지가 그대로
돌아오고**, 러너 로그에는 "해상도 미달"로 찍힌다. 크기만 보면 성공으로 집계된다.
러너의 지문 대조가 이걸 잡지만, 로그의 "해상도 미달"이 곧 "안전 필터 거부"라는 것을 알고 읽어야 한다.

또 하나: **다른 작업자의 세션 로그에서 엉뚱한 그림을 회수한 사례가 한 번 있었다**(TASK-ID 필터가
있는데도). 산출물 해상도가 1024가 아니라 1254로 찍히면 의심하고 눈으로 확인한다.

### 3.2 우회는 구글 제미니 — 다만 발주 문구를 바꿔야 한다

`agy`(Antigravity CLI, `.claude/skills/agy-antigravity/`)로 넘기면 거부 없이 나온다.
디즈니·알리·조니 뎁·다프트 펑크·빌리 아일리시·비욘세·셰어가 전부 이쪽에서 해결됐다.

단 제미니도 **"실존 인물의 얼굴을 그대로 재현하라"** 는 취지는 거부한다.
**"첨부한 사진 한 장을 고화질로 복원하고 다시 구도를 잡아라"** 는 사진 보정 작업으로 표현을 바꾸면 통과한다.

### 3.3 기존 서비스 아바타를 참조로 쓰면 다른 사람이 나온다

1차 재생성에서 7명이 "본인이 아니다"로 반려됐다. 공통 원인은 **기존 아바타를 참조로 쓴 것**이었다.
원본이 저화질·측면·손으로 얼굴을 가린 사진이면 모델이 얼굴을 지어낸다.

공식 사진을 새로 확보해 참조로 넣자 같은 도구로도 한 번에 통과했다(에드워드 노튼·앤드류 펠드만·이영표).
**구도·화질이 나쁜 인물은 처음부터 외부 공식 사진을 참조로 쓰는 편이 빠르다.**

관련: 업로드 스크립트가 기존 R2 아바타 URL을 `--identity-evidence` 로 받지 않는다(정책상 독립 근거가 아니다).

### 3.4 어두운 옷은 사이트 배경에 묻힌다

서비스 배경이 `#0a0a0a` 다. 검은 옷을 입힌 결과물은 배경을 지우면 **어깨가 통째로 사라지고 얼굴만 떠 보인다.**
김소월(어깨 밝기 8~11)과 셰어(5)가 이 이유로 한 번 반려됐다. 발주 단계에서 밝은 복식을 지정하는 편이 안전하다.

검수는 반드시 **밝은 배경 · `#0a0a0a` · 원형 크롭** 세 화면에서 한다. 밝은 배경만 보고 통과시키면 안 된다.

### 3.5 원형으로 잘릴 때 정수리가 닿는다

머리 위 여백이 15~30px이면 서비스의 원형 표시에서 정수리가 잘린다. **50px 이상**을 목표로 한다.
발주서에 "머리 위 여백"을 명시해도 모델이 증명사진 구도로 수렴하는 경향이 있어, 검수에서 잡아
축소·하향 배치로 조정한 사례가 3건 있다(비욘세·셰어·뭉크).

### 3.6 배경 제거 도구는 입력 폴더가 하나뿐이다

`C:\project\nobg\batch\batch_work\originals\` 고정이라 **여러 작업자가 동시에 쓰면 서로의 파일이 섞인다.**
이번에 남의 파일 한 쌍이 소실된 정황이 있었다. 시작 전에 남의 파일이 있으면 지우지 말고 그대로 두고
내 파일만 추가·회수한다. webp만 읽으므로 png를 넣으면 조용히 0장 처리된다.

---

## 4. 남은 일

### 4.1 김소월 — 얼굴 자체를 다시 볼지 판단 필요

이번엔 복식 색만 바꿨다(어두운 옷 → 아이보리). 그런데 **등록된 얼굴이 위키데이터 P18의 실제 사진과
차이가 있다**(실제 쪽이 더 둥글고 넓은 얼굴). 기존 승인 얼굴을 유지한 결과라 그 차이가 그대로 남았다.
얼굴을 실제 사진 기준으로 다시 잡을지는 별도 판단이 필요하다.

- slug `kim-so-wol` / celeb_id `f2e4e2d4-3d58-45b0-8df4-4108aef81eba`

### 4.2 해서 · 공융 — 사용자 검토 대기

둘 다 얼굴 근거가 없어 이전 회차에서 아바타를 내렸고 업로드 검역 대상이다.
사용자가 직접 판단하겠다고 보류한 상태다. 임의로 등록하지 마라.

- `hai-rui` `kong-rong` — 근거는 `celeb-avatar-defects.md` 참조

### 4.3 제프 딘 — 신원 이견이 기록된 채로 반영됨

사용자가 지정한 영상 자산 폴더의 사진(`sw/remotion/public/factions/AI-Supremacy/02-google-deepmind/2/03-jeff_dean/제프 딘.png`)을
참조로 써서 반영했다. 다만 **그 사진 속 인물이 공개된 제프 딘 본인(마르고 긴 얼굴, 희끗한 머리)과
다르게 보인다**는 점을 작업 중 사용자에게 보고했고, 사용자가 그 파일로 확정했다.
영상용 자산 자체가 잘못 잡혔을 가능성이 남아 있다.

- slug `jeff-dean` / celeb_id `a8c0d1e2-3f45-6789-abcd-ef0123456789`

### 4.4 본류 전수 정비는 그대로 남아 있다

`todo_celeb_avatar_overhaul.md` 의 확정 결함 잔여 명단과 probable 148명 분류는 이번 회차와 별개다.
이번에 처리한 57명 중 그 명단과 겹치는 인물이 있으므로, **본류를 재개할 때 `celeb-avatar-defects.md`
와 기계 큐에서 이번 완료분을 먼저 제거**해야 한다. 이번 회차에서는 그 정리를 하지 않았다.

---

## 5. 미완 — 톰 행크스 · 완적 (재개 필요)

사용자 지시로 아래 2명의 재작업에 들어갔으나 **이미지 생성 할당량이 소진돼 중단**됐다.
둘 다 서비스에는 손대지 않았으므로 현재 상태 그대로다. 할당량이 회복되면 §7 절차로 재개한다.

| 인물 | slug | celeb_id | 해야 할 일 |
|---|---|---|---|
| 톰 행크스 | `tom-hanks` | `3db04c68-8600-4a12-b690-4a080d3e619a` | **등록된 얼굴이 본인이 아니다.** 위키미디어 등에서 이름이 명시된 공식 사진을 새로 확보해 참조로 쓰고 재생성. 기존 아바타를 참조로 쓰면 안 된다 |
| 완적 | `ruan-ji` | `8f3485fc-adb1-4eb7-805a-a0726f0ae61b` | 반영은 됐으나 얼굴·옷에 대나무 잎 그림자가 얼룩져 원형에서 산만하다. 현재 아바타를 참조로 쓰되 **고르고 부드러운 조명**으로 다시 뽑는다 |

신원 근거 URL: 톰 행크스 `https://www.wikidata.org/wiki/Q2263` · 완적 `https://www.wikidata.org/wiki/Q142683`

이미지 생성 할당량은 텍스트 모델과 별개로 소진된다. 기다리는 것 외에 우회는 없다
(OpenAI가 막히면 제미니, 제미니도 막히면 대기).

---

## 6. 파일 위치

작업 산출물(생성 원본 57장, 대조표, 그룹별 발주서, 참조 사진, 되돌림용 사본)은
**2026-07-31 전량 삭제했다.** 서비스 반영과 R2 역검증이 끝나 보관 이유가 없다.
최종본은 R2 `celebs/{id}/avatar.webp` 에만 있다.

남아 있는 것은 다음 둘이다.

| 경로 | 내용 |
|------|------|
| `육안검사.txt` | 사용자가 적은 원본 결함 명단(저장소 루트) |
| `sw/web-bo/scripts/regenerate-celeb-avatars.mjs` | 재생성 러너(§2). 다음 회차에도 쓴다 |

재개할 때는 배치 JSON을 새로 쓰면 된다. 형식은 §2에 있다.

---

## 7. 작업 절차 (이번 회차에서 굳어진 순서)

1. 대상 인물의 결함 유형을 분류한다(회화 / 흑백 / 구도 / 저화질 / 신원 오류 / 시기 요구).
2. **신원 오류·저화질·측면 원본은 외부 공식 사진을 먼저 확보**한다(§3.3). 그 외는 기존 아바타를 참조로 쓴다.
3. 배치 JSON을 만든다. `note` 는 **인물마다 다르게** 쓴다(조명·표정·복식). 얼굴 생김새는 쓰지 않는다.
4. 러너로 생성한다. `--concurrency 1`.
5. 생성물을 **전량 Read로 열어** 검수한다. 회화체·흑백·옆모습·잘림·과확대·소품·복수 인물·신원 불일치.
6. 막히면 제미니로 우회한다(§3.2).
7. 배경 제거(전용 도구, CPU 단일).
8. 800×800 RGBA WebP 품질 100으로 변환.
9. **밝은 배경 · `#0a0a0a` · 원형** 세 화면 검수.
10. `upload-celeb-image-from-wikimedia.ts` 로컬 파일 모드로 업로드
    (`--face-detect false --size 800 --quality 100 --preview-path`).
    `--identity-evidence` 는 외부 공식 URL이어야 한다(기존 R2 아바타는 거부된다).
11. R2 재다운로드 후 SHA-256 대조, DB `avatar_url` 재조회.
12. 전체가 끝나면 `POST /api/revalidate` 에 `tag=celebs` 로 캐시 갱신 1회.
13. 중간 산출물과 배경 제거 도구의 작업 폴더를 비운다.
