# 유입·행동 실측 감사 (2026-07-25)

> **26.08.10 후속:** Google의 브랜드 검색에서도 홈페이지·인물 허브가 사라졌고, URL은 색인 `PASS`인 채 property 전체 노출이 더 줄었다. 최신 수치와 브랜드 메타·구조화 데이터 교정은 `seo.md`의 「브랜드 검색 노출 붕괴」 절만 갱신한다. 이 문서의 90일 유입 수치는 07.25 기준선으로 보존한다.

서치콘솔·애널리틱스 양쪽을 직접 조회해 **사람이 어디서 오고 무엇을 하는지**를 확정한 보고서다.
이 문서의 수치는 전부 API 실측이며, 조회 방법도 함께 적어 재현 가능하게 둔다.
추정에 해당하는 부분은 「미확인」으로 따로 표시했다 — 확정과 섞지 말 것.

## 결론 두 줄

1. **규모는 밑바닥이다** — 90일 2,331명, 하루 평균 26명. 어떤 비율을 논해도 이 절대 규모를 먼저 봐야 한다.
2. **그 밑바닥마저 구글이 아니라 네이버에서 온다** — 네이버 91% 대 구글 1.6%. 그런데 그동안의 검색 대응은 구글 쪽에 몰려 있었다.

## 조회 수단 (재현 방법)

| 대상 | 수단 | 상태 |
|------|------|------|
| Search Console | MCP 서버 `google-search-console` | 연결됨. 접근 자원은 `sc-domain:feelandnote.com` 하나(권한 `siteFullUser`) |
| GA4 | 전용 MCP 없음. 서비스계정 JWT로 Data API 직접 호출 | 동작 확인(HTTP 200). SDK(`@google-analytics/data`) 미설치라 `node:crypto`로 JWT 서명 → `analyticsdata.googleapis.com/v1beta/properties/526353156:runReport` |

크리덴셜은 `sw/web/credentials/ga-service-account.json`(추적 제외), 속성 `526353156`.
**열람 전용만 가능** — Admin API가 미활성이라 GA4 설정 변경(맞춤 측정기준 등록 등)은 API로 못 한다.

## 실측 — 최근 90일 (2026-04-26 ~ 07-24)

### 총량

| 지표 | 값 |
|------|-----|
| **하루 평균 사용자** | **약 26명** — 규모 판단의 출발점 |
| 사용자 | 2,331 (신규 2,323) |
| 세션 | 2,627 |
| 페이지뷰 | 3,702 |
| 페이지/세션 | 1.41 — **이 서비스의 참여도 지표로 쓰지 마라**(아래 「행동」절) |
| 참여율 | 63.4% |
| 평균 세션 시간 | 89초 |

최근 14일은 일 24~52명. 앞 7일 254명 → 뒤 7일 191명(**-25%**). 주별로는 22주(5월 말)에 58→234로 급등한 뒤 230~260 정체.
급등 원인은 **미확인**(네이버 노출 변화 추정. 확인하려면 네이버 서치어드바이저 유입 추이를 봐야 한다).

### 유입 경로 — 네이버 91%

| 소스/매체 | 세션 | 비중 |
|-----------|------|------|
| `m.search.naver.com` / referral | 1,896 | 72% |
| `naver` / organic | 486 | 18.5% |
| `(direct)` | 115 | 4.4% |
| **`google` / organic** | **43** | **1.6%** |
| `youtube.com` / referral | 31 | 1.2% |
| `bing` / organic | 17 | 0.6% |
| 그 외(vercel·블로그·페이스북 등) | 39 | 1.5% |

국가는 한국 2,519 / 중국 39 / 미국 31. 기기는 모바일 1,868(71%) / 데스크톱 694 / 태블릿 71.
모바일 체류 63초 vs 데스크톱 165초.

### 네이버 유입은 검색인가, 자체 블로그 경유인가 — 검색이다

`m.search.naver.com`이 organic이 아니라 referral로 잡히므로 "블로그에 걸어둔 링크로 들어온 것 아닌가"를 먼저 갈라야 한다. 랜딩 분포로 판별한 결과 **검색 유입이 맞다.**

| 증거 | 실측 |
|------|------|
| 블로그·링크 경유 총량 | **16세션(0.6%)**. `m.blog.naver.com` 9 · `blog.naver.com` 3 · `link.naver.com` 1 · `m.naver.com` 2 · `naver.com` 3 |
| 그 16건의 랜딩 | 대부분 홈(`/`) 10건 — 블로그에 걸린 링크가 홈이라는 뜻. 인물 상세로 온 건 5건뿐 |
| `m.search.naver.com` 랜딩 종수 | **480종** |
| `naver` organic 랜딩 종수 | **196종** |
| 인물 상세 랜딩 종수(전체 출처) | **516종** — 516명의 인물 화면이 각각 유입을 받았다 |
| 시간대 분포 | 새벽 3~6시 23~34세션 / 낮·저녁 10~22시 115~157세션. 사람의 생활 리듬이며 특정 시각 몰림 없음 |
| 신규 대 재방문 | 2,195 대 142(6%) — 단골이 반복 방문하는 형태가 아니다 |

블로그 글 몇 편이 원인이라면 랜딩이 소수 인물에 몰려야 한다. 480~516종으로 퍼지는 분포는 그것으로 설명되지 않는다.

> 다만 **어떤 검색어로 노출되는지는 여전히 미확인**이다. 네이버는 GA에 검색어를 넘기지 않으므로 서치어드바이저 화면에서만 볼 수 있다.
> `m.search.naver.com`이 referral로 분류되는 이유(GA4의 네이버 모바일 검색 인식 한계)는 이 프로젝트에서 검증한 사실이 아니라 일반적으로 알려진 동작이다 — 판별 근거로 쓴 것은 위 랜딩 분포다.

### Search Console — 구글 실적은 사실상 0

- 90일 클릭 **8**, 노출 약 60. 노출된 질의는 3개뿐(`feel note` 33위, `sara blakely religion` 92위, `지브스와 우스터` 73위).
- 클릭 8회는 전부 루트(`/`)에서 발생. 인물 상세는 노출만 있고 클릭 0.
- 사이트맵: 15,884건 제출, 오류 0, 마지막 다운로드 07-17. (표시되는 `indexed: 0`은 이 API 필드가 값을 채우지 않는 항목이라 색인 수의 근거로 쓸 수 없다.)
- `index_inspect` 실측 — `/celeb/jensen-huang`: 판정 PASS(색인됨), 그러나 **마지막 크롤 2026-05-18**. 두 달 넘게 재방문 없음.

> `adsense-audit-2026-07-15.md`의 "회복 신호 없음"(07-22 관측)이 07-25에도 그대로다.

### 행동 — 페이지/세션으로 재지 마라 (초판 판정 철회)

| 진입 지점 | 세션 | 페이지/세션 | 이탈률 | 체류 |
|-----------|------|-------------|--------|------|
| 인물 상세(`/celeb/*`) | 2,358 (90%) | 1.22 | 34% | **78초** |
| 홈(`/`) | 160 (6%) | 4.76 | 39% | 265초 |

🔴 **이 문서 초판은 "1.22장 = 최악"이라 판정하고 홈의 4.76장과 대조해 "4배 차이"라 썼다. 그 판정을 철회한다.** 이유 둘:

1. **두 화면의 설계 목적이 다르다.** 인물 상세는 서가·관계망·영향력·인물 분석·독백·대사·영상·세력·방명록을 **한 장에 다 담은** 화면이다(`CelebPageContent.tsx` 칸 11개). 한 장에서 끝나도록 만든 화면이 1.22장인 것은 설계대로다. 홈은 여러 화면으로 흩어보내는 갈림길이라 성격이 반대다. 목적이 다른 둘을 같은 자로 재고 우열을 매겼다.
2. **겹창이 26.07.24에 도입됐다.** 인물 카드를 겹창(`CelebDetailModal`)으로 훑는 흐름은 이 90일 데이터 **기간 밖**이다. 겹창은 화면 전환을 의도적으로 줄이는 장치이므로 앞으로 페이지/세션은 **더 낮아지며, 그것이 개선의 결과다.** 이 지표로 참여도를 재면 개선을 후퇴로 읽는다.

**대체 지표** — 인물 화면은 체류(78초)·칸 도달·겹창 열림 수로 잰다. 한 장에 그만큼 담은 화면에서 78초는 짧으며, 이쪽이 실제 진단축이다.

네이버 유입 상위 랜딩: 젠슨 황 134 · 마이클 잭슨 120 · 다리오 아모데이 119 · 이재용 94 · 앤드루 휴버먼 72.

### 정착률

- 신규 2,327 vs 재방문 131 → **재방문 5.3%**. 단 재방문자 체류는 236초(신규 76초의 3.1배) — 붙은 소수는 깊게 쓴다.
- 로그인 화면 도달 90일 **7명**(12조회). 검색 화면 7명(15조회). 광장 전체 40여 조회.

### 계측 공백 (이번 작업으로 해소 착수)

28일 이벤트: `page_view` 1,295 · `session_start` 999 · `first_visit` 870 · `user_engagement` 851 · `scroll` 285 · **`click` 7**.
`gtag`/`sendGAEvent` 호출 코드가 코드베이스에 **한 건도 없었다** — 잡히던 건 전부 GA 자동 수집이다.
즉 인물 화면에서 ①어디까지 내려보는지 ②겹창을 여는지 ③아무것도 안 누르는지를 **판별할 수단이 없었다.**
겹창(26.07.24 도입) 흐름은 화면 전환을 남기지 않으므로 **자동 수집만으로는 원리적으로 안 보인다** — 계측 없이는 겹창 도입 효과를 평가할 방법이 아예 없다.

### 확인 필요한 이상값 (미확인)

- `/celeb/otto-von-bismarck`: 28세션·이탈 93%·활성 1명·29조회. 사람 아닌 접속 의심이나 미확인.
- `/celeb/andy-lau` 이탈 75%, `/celeb/monica-bellucci` 체류 17초 — 얇은 페이지인지 접속 성격 문제인지 미확인.
- 랜딩 `(not set)` 60~81세션, 이탈 98% — 계측 누락 세션.

## 구조 진단 (코드 실측)

인물 상세(`CelebPageContent.tsx`)의 다음 행선지 구조를 읽은 결과:

1. 맨 위 안내판(`CelebServiceNavigator`)은 **같은 화면 안 스크롤 이동 전용**이다(`scrollIntoView`). 다른 인물로 가는 길이 아니다.
2. 다른 인물을 보는 길은 네 칸에 흩어져 있고 전부 겹창을 거친다. 각 칸의 선정 기준은 서로 다르다.

| 칸 | 선정 기준 |
|----|-----------|
| 03 관계망 | 위키데이터에 기록된 실제 관계(혈연·사제·동료·대립) |
| 04 동시대 인물 | 생몰 기간이 겹치는 인물 중 **출생 연도가 가까운 순** 30명(`MAX_CONTEMPORARIES`). 화면 노출은 모바일 3·PC 6 + 더보기 |
| 06 인물 분석 속 닮은 인물 | 성향 16축 벡터 거리 최근접 5명 |
| 10 세력도감 | 그 인물이 배정된 태그(그룹)의 동료 |

3. 화면 마지막 칸이 방명록이었다 — 다 읽고 내려온 사람에게 다음 행선지가 없었다.

⚠️ **초판은 "겹창이 2단계라 이동을 막는다"를 1.22장의 유력 원인으로 적었다. 철회한다.**
겹창은 **26.07.24에 의도적으로 도입한 개선**이며(매번 화면을 통째로 전환하면 피곤하다는 판단), 90일 데이터 기간에는 존재하지 않았다. 없던 장치를 그 기간 행동의 원인으로 지목한 오류다. 겹창을 줄이거나 우회시키는 방향을 이 데이터로 정당화하지 마라.

3번(마지막 칸 뒤 행선지 부재)만 데이터와 무관하게 확정적인 결함이었다.

## 이번 작업으로 반영한 것

### ① 행동 계측 (신규)

`sw/web/src/lib/analytics/track.ts` — `sendGAEvent` 래핑. 전송 실패는 화면 동작을 막지 않고 개발 중에만 콘솔 경고.

| 이벤트 | 매개변수 | 답하는 질문 |
|--------|----------|-------------|
| `celeb_section_view` | `section` | 어느 칸까지 내려보는가 (`IntersectionObserver`, 칸마다 1회, 문턱 1%) |
| `celeb_guide_click` | `section` | 맨 위 안내판에서 무엇에 관심을 보이는가 |
| `celeb_person_open` | `source` (`contemporaries`·`persona`·`faction`·`relations`) | 어느 칸이 인물 탐색으로 이어지는가 |
| `celeb_person_go` | `to`, `from` | 겹창·끝 칸에서 실제로 다른 인물로 넘어가는가 |
| `celeb_voice_play` | `kind` (`greeting`·`quote`) | 음성 기능을 쓰는가 |

문턱을 1%로 잡은 이유 — 서가처럼 화면보다 큰 칸은 40% 같은 값을 영원히 채우지 못한다.

### ② 화면 끝 "이어서 볼 인물" — 만들었다가 같은 날 철회

방명록 뒤에 동시대 인물 4명을 겹창 없이 곧바로 이동하는 링크로 깔고 인물 목록 링크를 곁들였다. **같은 날 전량 철회했다.** 이유 둘:

1. **재료가 04번 동시대 인물 칸과 같았다** — 새 선정 기준이 아니라 04번 목록의 앞 4명 재사용이라, 같은 얼굴이 한 화면에 두 번 나왔다.
2. **겹창 설계 의도와 정반대였다** — 겹창은 매번 화면을 통째로 전환하면 피곤하다는 판단으로 26.07.24에 만든 장치다. 그 방향을 되돌리는 "바로 이동"을 굳이 심을 이유가 없다.

철회 범위: 부품 파일 삭제 · `CelebPageContent` 원복 · 문구 3종 제거 · `CelebPersonPreviewButton`의 `href` 확장 원복(git 대조로 무변경 확인). 계측(①)은 유지한다.

> **남은 사실**: 화면 마지막 칸이 방명록이고 그 뒤 행선지가 없다는 점은 그대로다. 어떤 형태로 채울지는 계측 결과(칸 도달률·겹창 열림 출처)를 보고 정한다. **인물 화면에 이동 경로가 부족한 것이 아니다** — 04·03·06·10 네 칸이 각각 다른 기준으로 인물을 제시한다.

검증: `tsc --noEmit` 통과(철회 후 재실행) · 변경 파일 `eslint --quiet` 신규 오류 0.

> `CelebDetailModal.tsx`에 린트 오류 2건이 있으나 **원본에 이미 있던 것**이다(`git show HEAD:` 대조 확인) — effect 안 setState 동기 호출(58행대), 렌더 중 컴포넌트 정의(`FollowButton`·`ProfileLink`·`MetaInfo`). 후자는 상태 초기화 버그 위험이 있어 별도 정리 대상.

## GA4 설정 — 완료분 (26.07.25)

**맞춤 측정기준 5종 등록 완료.** Admin API가 미활성이라 API로는 등록할 수 없어 관리 화면에서 수동 등록했고, Data API 메타데이터로 실측 확인했다.

| API 이름 | 표시명 | 조회 시험 |
|----------|--------|-----------|
| `customEvent:section` | 칸 이름 | 통과 |
| `customEvent:source` | 겹창 출처 | 통과 |
| `customEvent:to` | 이동 대상 | 통과 |
| `customEvent:from` | 이동 출처 | 통과 |
| `customEvent:kind` | 음성 종류 | 통과 |

`to`·`from`이 예약어와 충돌하지 않음도 이때 확인됐다.

**데이터 보관 2개월 → 14개월 변경 완료**(무료 등급 최대치). 기본값 2개월이면 세부 기록을 두 달 뒤 거슬러 볼 수 없었다.

⚠️ **맞춤 측정기준은 등록 이전 데이터에 소급되지 않는다.** 그래서 배포보다 등록이 먼저였다. (이 문서 초판에 "등록 전에도 탐색 분석에서는 조회된다"고 적었던 것은 오류이며 삭제했다.)

## 남은 일 — 코드로 할 수 없는 것

1. **네이버 서치어드바이저 점검** — 유입 91%인데 관리 도구 상태가 미확인이다. 소유 확인 표식은 코드에 있다(`[locale]/layout.tsx`의 `naver-site-verification`). 사이트맵 제출·수집 현황, 5월 말 급등 원인, 검색 질의는 그 화면에서만 볼 수 있다.
2. **구글 우선순위 재검토** — AdSense 재신청 게이트가 구글 색인 회복을 조건으로 걸고 있다. 유입 1.6% 채널에 어느 만큼의 공정을 계속 투입할지는 판단 사항. (AdSense 자체는 구글 심사이므로 색인 문제와 별개로 남는다.)
3. **이상값 판별** — 위 「확인 필요한 이상값」 3건.

## 재조사용 스크립트

초판(26.07.25) 조회에 쓴 스크립트는 세션 임시 폴더에 있어 보존되지 않았다. **26.07.27에 인물 화면 조회용으로 다시 작성해 저장소에 넣었다** — `sw/web/scripts/ga4-celeb-views.mjs`. 서비스계정 JWT → `runReport`, 의존성 없음.

```bash
node scripts/ga4-celeb-views.mjs 2026-06-26 2026-07-27   # 인물별 페이지뷰·순 방문자·접속 수
```

다른 지표(랜딩 분포·유입 채널 등)가 필요하면 이 파일의 `runReport` 본문에서 `dimensions`·`metrics`·`dimensionFilter`만 바꿔 쓴다. 새로 짜지 말 것.

**26.07.27 대조 결과** — 인물 화면 조회수는 GA4와 DB가 사실상 일치한다(같은 기간 GA4 1,144 / DB 1,200, 인물별 오차 ±3 이내). DB가 조금 더 큰 것은 광고 차단으로 GA4가 못 잡는 접속까지 세기 때문이다. 즉 **DB 조회수는 보정 없이 그대로 신뢰해도 된다** — 중복 제거 창이 24시간이던 시절에도 걸러낸 양이 거의 없었다(재방문율 5.3%). 창은 26.07.27에 30분(웹 분석 표준)으로 조정했다.
