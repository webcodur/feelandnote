/** 세력도 편집 세부 페이지 경로 값 — /[series]/[name]/[lang]/[tab] 의 허용 세그먼트 */
export const FACTION_EDIT_LANGS: ReadonlySet<string> = new Set(['ko', 'en', 'both'])
export const FACTION_EDIT_TABS: ReadonlySet<string> = new Set(['info', 'layout'])
