# 삼국지 셀럽 아바타 정비 인수인계

> 작성일: 2026-07-31  
> 중단 지시: 현재 진행 중인 위연·장굉·능통까지만 완결하고 이후 작업은 시작하지 않는다.  
> 재개 시 이 문서를 먼저 읽고, 완료 인물을 다시 작업하지 않는다.

## 1. 이번 작업의 결론

문제는 파일 형식이 아니라 **실제 아바타 구도**였다.

- 기존 검수는 800×800, RGBA, 누끼, 업로드 성공 여부를 주로 확인했다.
- 상반신·흉상 원본을 통째로 정사각 캔버스 안에 넣는 `contain`형 처리가 섞이면서 인물이 중앙에 작게 떠 버렸다.
- 정면 시선, 얼굴 점유율, 쇄골 이하 노출, 원형 크롭의 실사용 상태를 성공 조건으로 보지 않은 것이 재발 원인이다.
- `_refs`와 실제 R2 아바타는 항상 같은 파일이 아니므로, REF만 보고 서비스 상태를 판정하면 안 된다.

2026-07-31 실제 R2 등록본을 내려받아 `_refs` 인물과 매칭한 56명을 육안 감사했다. 명백한 통과는 약 16명, 교체 대상은 약 40명이었다. 이 수는 당시 스냅샷이며 아래 완료 작업을 거치면서 줄었다.

## 2. 앞으로의 합격 기준

아래 항목을 모두 통과해야 등록한다.

1. 사용자 지정 원본 또는 canonical REF의 얼굴 구조, 눈·코·입, 수염, 머리장식, 복식 색 배치를 보존한다.
2. 고개는 정면이고 양쪽 눈이 카메라를 직접 본다. 큰 3/4 각도, 옆·위·아래 시선은 불합격이다.
3. 머리·관모·투구의 핵심 형태는 정사각 자산 안에 온전히 남긴다.
4. 얼굴·목·어깨 윗선만 담는다. 쇄골, 가슴, 긴 갑옷 상체, 손은 보이지 않게 한다.
5. 머리 끝은 상단에 가깝고 어깨는 좌우 또는 하단 경계로 자연스럽게 이어져야 한다.
6. 인물이 캔버스 중앙에 섬처럼 떠 있거나 바깥 투명 여백이 넓으면 불합격이다.
7. 800×800 sRGB RGBA WebP로 만들고 밝은 배경, `#0a0a0a`, checker, 실제 원형 마스크를 모두 육안 검수한다.
8. 원본·현재 서비스본·후보를 한 화면에서 비교한 뒤 총괄 승인을 받아야 한다.
9. 승인 전에는 DB/R2에 쓰지 않는다.
10. 승인 후 같은 담당자가 R2 역다운로드의 HTTP 200, MIME, 800×800 RGBA, SHA-256 일치를 확인한다.
11. 기존 프로필은 `avatar_url` 외 필드를 바꾸지 않는다. 신규 최소 프로필은 요청대로 내용 필드를 비운다.
12. 실존 역사 인물은 QID·정사·명명 도상 또는 명시적 시대 재구성 근거를 남긴다. `_refs`만 신원 증거라고 주장하지 않는다.

### 재생성 여부

- 고해상도 정면 원본으로 해결 가능하면 새 얼굴을 만들지 말고 결정적 crop/recompose를 우선한다.
- 원본 자체가 비정면이거나 해상도가 부족하거나 어깨 연결이 불가능할 때만 identity-preserve 재생성을 한다.
- 업로드 도구가 승인된 프레임을 다시 `contain` 또는 자동 얼굴 크롭으로 망치지 않는지 원격 파일로 재확인한다.

## 3. 작업 운영 규칙

- 작업 단위는 **인물 1명 종단간**이다.
- 한 담당자가 조사 → 후보 제작 → 누끼 → 자체 QA → 총괄 승인 요청 → 등록 → 역검증 → 정리까지 책임진다.
- 총괄은 제작하지 않고 원본 대비 완성본을 검수해 승인 또는 반려한다.
- 생성·조사는 병렬로 해도 되지만 `C:\project\nobg` 누끼는 CPU 단일 프로세스만 사용한다.
- `nobg\batch\batch_work\originals`와 `nobg` 출력 폴더에 다른 사람 파일이 섞이지 않게 하고 자기 작업 뒤 비운다.
- R2·DB 검증이 끝나면 생성 원본, 실패본, 비교판, 누끼 입력·출력, 임시 업로드 파일을 삭제한다.
- 사용자 원본 `C:\Users\webco\바탕 화면\sam`은 절대 삭제하거나 덮어쓰지 않는다.

## 4. 이번 교정 완료

아래 인물은 원본 대비 최종 검수, DB/R2 교체, 원격 역검증, 캐시, credits, 임시 정리까지 완료했다.

| 인물 | slug | profile id | 결과 |
|---|---|---|---|
| 방덕 | `pang-de` | `04970b62-32f7-4c66-b8e6-db42a47a6a72` | 정면·어깨 윗선 교정 완료 |
| 도겸 | `tao-qian` | `dbd82a84-4dce-4267-a50b-dfaad44917ee` | 위쪽 사선 시선 → 정면 교정 완료 |
| 마등 | `ma-teng` | `feeb7ad6-f048-4a0f-8531-18ae1685c16e` | 3/4 고개 → 정면 교정 완료 |
| 순욱 | `xun-yu` | `c0fb93c0-261a-4e86-9b49-16397fe6ea06` | 피사체 38.86% → 53.09%, active/full의 avatar만 변경 |
| 견부인 | `lady-zhen` | `f06d166e-d460-481f-992d-aed9a4b5ab3f` | `sam/견희.webp` 직접 identity 복원, 39.0% → 54.8% |
| 후한 헌제 | `emperor-xian-of-han` | `dd5c9d25-7ad5-4ee5-a945-ac3fd248caef` | 피사체 29.9% → 53.3% |
| 유기 | `liu-qi` | `ae5acf2a-3d09-4b8c-96cc-5e386c8941d2` | 피사체 25.54% → 43.65%, 비정면 → 정면 |
| 전위 | `dian-wei` | `633926d3-1607-4cdf-9030-b63c253f0095` | 기존 픽셀 deterministic crop, 긴 갑옷 상체 제거 |
| 유표 | `liu-biao` | `6e01e0c1-8094-4918-b80b-49a5086683ab` | 작은 비스듬한 흉상 → 정면 근접 컷 |
| 위연 | `wei-yan` | `ec2092f1-df3e-491f-a592-62ec7344f715` | 정면 직시·얼굴/어깨 점유율 교정, 승인 SHA 원격 일치 |
| 장굉 | `zhang-hong` | `26e276b5-d783-476a-95c0-0e32e436ebc1` | 정면 근접 컷 교정, 승인 SHA 원격 일치 |
| 능통 | `ling-tong` | `97f1f8b0-8a55-4ca4-beb5-7382b8bc232c` | 붉은 두건·정면 직시·어깨 윗선 보존, 승인 SHA 원격 일치 |

신규 등록 완료:

| 인물 | slug | profile id | 상태 |
|---|---|---|---|
| 손상향 | `sun-shangxiang` | `f22428af-3bf3-4f92-94b8-c1ff36a6d9cf` | CELEB / light / inactive, 내용·연관 데이터 비움 |

위연·장굉·능통까지 최종 처리한 뒤 사용자 지시대로 유장·전예·여포와 이후 인물은 시작하지 않고 중단했다.

## 5. 육안 감사 후 남은 교체 대상

아래 명단은 현재 진행 중인 위연·장굉·능통과 이미 완료한 인물을 제외한 잔여다. 재개 시 한 번에 배치 등록하지 말고 인물 단위로 처리한다.

### 명백한 원경·긴 상체·외곽 여백 또는 비정면

- 가후
- 감녕
- 공손찬
- 곽가
- 관평
- 대교
- 동탁
- 마대
- 마속
- 마초
- 맹획
- 소교
- 손견
- 손책
- 순유
- 악진
- 안량
- 여포
- 왕평
- 요화
- 유선
- 유장
- 장료
- 장합
- 전예
- 정보
- 조운
- 주태
- 태사자
- 하후돈
- 혜강
- 황개
- 황충

### 별도 재확인

- 공융: `sam/공융.jpg` 직접 identity 사용 여부를 원본·R2 나란히 다시 확인한다.
- 하후연·장소·초선 등 당시 통과로 본 인물도 최종 전수 감사에서 true-circle을 다시 확인한다.
- 서비스/DB 명단과 `_refs` 파일명이 완전히 일치하지 않는 인물은 수치 감사에서 빠질 수 있으므로 이름 매칭만 신뢰하지 않는다.

## 6. `sam` 폴더 남은 신규/재작업

원본 위치: `C:\Users\webco\바탕 화면\sam`

완료:

- 견희 → 견부인
- 도겸
- 마등
- 방덕
- 손상향

기등록, 재감사 필요:

- 공융
- 순유

아직 시작하지 않음:

- 사마사
- 사마소
- 장각
- 정봉
- 제갈근
- 화타
- 황월영

사용자가 이 폴더 명단 정도면 충분하다고 했으므로, 신규 확장은 이 명단을 먼저 끝내고 지나치게 깊은 인물은 임의로 늘리지 않는다.

## 7. 핵심 경로

| 용도 | 경로 |
|---|---|
| 사용자 직접 REF | `C:\Users\webco\바탕 화면\sam` |
| 삼국지 canonical REF | `C:\project\feelandnote\sw\remotion\public\factions\three-kingdoms\_refs` |
| WANTED | `C:\project\feelandnote\sw\remotion\public\factions\three-kingdoms\_refs\wanted.txt` |
| 업로드 스크립트 | `C:\project\feelandnote\sw\web-bo\scripts\upload-celeb-image-from-wikimedia.ts` |
| credits | `C:\project\feelandnote\sw\web-bo\scripts\celeb-image-credits.log` |
| 이미지 규격 | `C:\project\feelandnote\docs\project\db-celeb.md` |
| 이미지 발주 규칙 | `C:\project\feelandnote\docs\project\image-generation.md` |
| 누끼 도구 | `C:\project\nobg` |

서비스 최종본의 단일 보관처는 R2 `celebs/<profile-id>/avatar.webp`다. 로컬 작업물은 검증 뒤 남기지 않는다. canonical REF만 영상 자산으로 보존한다.

## 8. 종료 시점 상태

아래 세 인물까지 등록·역검증·정리를 마치고 작업을 종료했다.

| 인물 | 상태 | 최종 SHA | 비고 |
|---|---|---|---|
| 위연 | 완료 | `8FEDDA5DBAF765C9550C134682730C18FA0553E580CA12E26741A6EE972D8A05` | R2 800×800 sRGB RGBA, avatar 외 데이터 불변, 유장 미착수 |
| 장굉 | 완료 | `C523F88E340DF7D449B91B9C942B265345D65A25D0C370B623A65BEBFE4B3DA8` | R2 800×800 RGBA, avatar 외 데이터 불변, 전예 미착수 |
| 능통 | 완료 | `15EA56FA964D413168C30D2D27ED5752A03BD9A9D1C63ABE24DB9FBA3BB58C4A` | R2 800×800 sRGB RGBA, avatar 외 데이터 불변, 여포 미착수 |

종료 정리:

- 세 인물 모두 R2 재다운로드 HTTP 200, `image/webp`, 800×800 RGBA, 승인 SHA-256 일치, true-circle 육안 통과를 확인했다.
- 세 인물 모두 캐시 갱신, credits, canonical REF 반영을 확인했다.
- 후보·실패본·비교판·QA 시트·원격 다운로드본·임시 업로드 스크립트·누끼 입출력을 삭제했다.
- 이전 삼국지 회차에서 `nobg\batch\batch_work`에 남은 조인·초선·왕평·장소 중간 산출물 8개와 빈 `.tmp\celeb-avatar`도 정리했다.
- `generated_images`, 세 인물 작업 폴더, 관련 `nobg` 입출력 잔재는 0이며 사용자 `sam` 원본은 보존했다.

## 9. 재개 순서

1. §4와 §8의 완료 인물은 재작업하지 않는다.
2. §5 잔여 중 사용자에게 가장 잘 보이는 인물부터 한 명씩 교정한다.
3. `sam`의 공융·순유를 직접 identity 기준으로 재감사한다.
4. `sam` 미착수 7명을 인물 단위로 신규 처리한다.
5. 실제 R2 등록본을 다시 전수 다운로드해 true-circle과 정면 시선을 육안 감사한다.
6. 최종적으로 DB avatar 빈값, R2 형식·해시, credits 중복, `.tmp`·generated_images·nobg 잔재를 확인한다.
