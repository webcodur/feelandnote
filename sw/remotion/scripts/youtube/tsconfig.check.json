{
  "//": "youtube 업로드 타입검증. googleapis 가 @types/node 를 끌어와 node 내장 모듈 타입이 유입된다.",
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    "noEmit": true
  },
  "include": [
    "./**/*.ts",
    "../lib/**/*.ts"
  ]
}
