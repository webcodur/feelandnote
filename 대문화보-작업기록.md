# 인물 대문 화보 — 작업 기록 (2026-07-31)

인물 상세 페이지 맨 위에 큰 정사각 사진을 붙이는 작업을 했다. 이 파일은 **무슨 일이 있었고 어디까지 갔는지**를 남긴 것이다.
규격·도구·함정은 `docs/project/db-celeb.md`, 미진분과 재개 절차는 `docs/project/celeb/hero-photo-status.md`가 SSoT다.

---

## 시작과 결론

**질문**: "기본 아바타 말고 큰 이미지도 기획에 있었나?"

**확인 결과**: 세로 초상(9:16)이 기획됐다가 전면 제거돼 있었다. 주소만 817건 남아 있었는데 **원본 파일은 한 장도 없었다**(옛 저장소 버킷 852개 파일 중 portrait 이름 0개, 샘플 URL 400). 되살릴 수 없어 주소를 전부 비웠다.

**방향 전환**: 세로가 아니라 **정사각 1:1**로 다시 만들기로 했다. 이미 만들어 둔 화보가 그 규격이고, 세력도감 화보와도 같아서다.

---

## 만든 것

### 화면
인물 페이지 첫 구획의 동그란 얼굴 자리를, 화보가 있으면 **정사각 큰 사진**으로 바꿨다.
- 화보 없으면 기존 동그란 얼굴 그대로 (들쭉날쭉하지 않게)
- 대문이 비면 **세력도감 화보를 자동으로 끌어다 쓴다** → 전량을 안 채워도 화면이 깨지지 않는다
- 눌러서 인사 음성 듣는 기존 동작 유지
- `CelebHeroPhoto.tsx`(신설) · `CelebPageContent.tsx` · `getCelebBySlug.ts`

### 백오피스 도구 4종 (`sw/web-bo/scripts/`)
| 파일 | 하는 일 |
|------|---------|
| `upload-celeb-hero-photo.ts` | 손에 있는 파일을 대문에 등록 |
| `scan-faction-portrait-candidates.mjs` | 팩션 폴더에서 쓸 만한 인물 이미지 수집 |
| `pick-hero-photo-targets.mjs` | 대문 빈 인물을 방문·영향력 순으로 추출 |
| `generate-celeb-hero-photos.mjs` | 생성 → 진위검사 → 등록 → 로컬 정리를 한 건에 |
| `create-minimal-celebs.ts` | 이름만 있는 최소 인물 등록 |

---

## 채운 결과 (2026-07-31 기준)

**대문 보유 390명** (활성 351명 / 신화·관계 등급 뺀 활성 338명)

| 출처 | 인원 |
|------|------|
| 손에 있던 파일(`인물사진-11`) | 24 (등록 후 로컬 삭제, 폴더 비움) |
| 팩션 폴더에서 전용 | 85 |
| 생성 | 나머지 |

**아직 대문이 빈 인물 1,134명** (얼굴 사진은 있음)

### 생성 규모
성공 **247장** (중단된 배치 포함). 마지막 배치는 대상 181명 중 **완료 125 · 실패 2 · 건너뜀 54**로 끝났다.

실패 2건(밥 아이거·조니 뎁)은 모두 `해상도 미달 800x800` — 생성이 안 된 채 **넣어준 얼굴 사진이 그대로 돌아온 경우**다. 진위검사가 걸러냈다. 이 세션 전체로는 3건 잡았다(+ 마이클 잭슨).

---

## 겪은 함정 (다시 하면 또 걸린다)

1. **실사 질감 문구를 빼면 회화체로 나온다.** 첫 시험이 붓질 그림이었다. 러너의 `RENDERING` 블록이 그 방어막이고 지우면 안 된다.
2. **생성 도구가 시간 초과로 죽어도 그림은 이미 나와 있는 경우가 있다.** 실패를 바로 던지지 말고 회수를 먼저 시도한다. 이걸 안 해서 1건이 헛되이 실패 처리됐다.
3. **생성 실패 세션에서 결과를 뽑으면 넣어준 얼굴 사진이 그대로 돌아온다.** 예전에 1,141건 중 292건만 진짜였던 사고의 원인이다. 축소 지문 대조로 걸러야 하고, 실제로 이번에 1건(마이클 잭슨) 잡았다.
4. **"상반신~무릎"처럼 폭넓게 쓰면 원경 전신으로 나온다.** 대문은 240픽셀이라 그러면 얼굴이 안 보인다. 크롭을 못박아야 한다.
5. **앉은 인물에게 "무릎 위에서 잘라라"를 강제하면 역효과다.** 다리가 책상에 가려 무릎을 보이려면 카메라를 빼야 하고, 그러면 인물이 오히려 작아진다. "무릎을 보이려 하지 말고 화면을 채워라"로 보완했다.
6. **배치 명단을 뽑을 때 앞 배치를 반드시 제외해야 한다.** 병합 시점에 아직 안 끝난 연출이 있어 15명이 중복 배정됐다. 건너뛰기가 있어 사고는 안 났지만 연출 작업이 헛돌았다.

---

## 손대지 않은 것 (중요)

- **팩션·에피소드 원본 이미지는 하나도 지우지 않았다.** 영상 제작 자산이라 읽어서 복사만 했다. 로컬 삭제는 생성물과 `인물사진-11` 폴더에만 적용했다.
- **무함마드는 대상에서 뺐다.** 이슬람 전통에서 형상 묘사가 금기라 얼굴 참조로 새 그림을 만드는 이 방식과 맞지 않는다.

---

## 남은 일

1. **배포 전까지 화면에 안 뜬다.** 배포 후 인물 자료 갱신 신호를 한 번 보내야 즉시 반영된다(7일 저장 때문).
2. **크롭 규격이 통일돼 있지 않다.** 표본 24명 실측에서 목표 일치는 10~17%뿐이었다. 다만 **못 쓸 화보는 0명**이었고, 어긋남의 대부분은 목표보다 당겨진 쪽이라 급하지 않다. 실제 손해는 빠진 쪽(생성분 17%·팩션분 25%). 전수 검수는 하지 않았다 — 어느 인물이 어느 쪽인지는 미확인.
3. **codex 한도가 회복되면** `docs/project/celeb/hero-photo-status.md`의 절차대로 이어가면 된다. 연출 본문은 이미 만들어 둔 것이 남아 있다.
4. **미커밋** — 이 파일, `docs/project/celeb/hero-photo-status.md`, `AGENTS.md` 링크 한 줄.

---

## 이순신 건 (혼선이 있었음)

주신 명량 연설 컷으로 한 번 바꿨다가, 에피소드 48장을 훑어 **칼을 뻗어 호령하는 컷**으로 최종 교체했다. 그 과정에서 **원래 걸려 있던 생성본(명량 새벽 갑판, 두루마리 든 컷)을 덮어써 잃었다.** 다시 뽑으려 했으나 한도 소진으로 중단했다. 발주서는 남아 있으니 한도 회복 후 되살릴 수 있다.
