# GEMINI.md (AI 작업 절대 원칙)

## ❌ 브라우저 서브에이전트 사용 금지

`browser_subagent` 툴은 **절대 사용하지 않는다.** 느리고 실패율이 높다.
UI 확인이 필요할 때는 파일을 직접 읽거나(`view_file`), 이미지를 생성(`generate_image`)하여 판단한다.

## 🧹 임시 스크립트 파일 즉각 삭제

작업 편의를 위해 루트 디렉토리나 임의의 경로에 생성한 임시 스크립트 파일(`*.js`, `*.py` 등)은 해당 태스크가 끝나는 즉시 **반드시 삭제(제거)**하여 프로젝트 공간을 어지럽히지 않는다.