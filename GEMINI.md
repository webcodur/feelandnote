# GEMINI.md (AI 작업 절대 원칙)

지금 GEMINI CLI 입력 불가능할거다. 다 실패할 거임.

기본 읽기 쓰기 툴만 쓸 것

<!-- ## ❌ 절대 하지 말아야 할 방식 (에러/Hang 유발)
1. **대규모 파일 복사 시 Node.js 사용:** 대용량 파일(이미지 등) 다수를 `node script.js` 내부에서 `fs.copyFileSync`로 돌리면 Hang이 발생하므로 **절대 금지**.
2. **CLI 기반 깊은 탐색:** `grep -rn` 또는 `find -exec grep` 처럼 하위 디렉토리를 깊게 탐색하는 명령어는 타임아웃을 유발함.
3. **CMD 명령어 혼용:** Bash 쉘에서 `dir /s /b` 같은 Windows cmd 전용 명열어를 사용하면 경로 오류가 발생함.

## ✅ 반드시 이렇게 할 것 (100% 성공 공식)
1. **파일의 단순 복사 및 이동:** 무조건 **개별 `cp` 명령어**를 여러 줄로 작성하여 `run_command`로 실행한다. (예: `cp file1.png path/ && cp file2.png path/`)
2. **파일 검색 및 텍스트 탐색:** CLI 명령어에 의존하지 말고, 본연의 **AI 전용 도구(`list_dir`, `grep_search`)**를 무조건 최우선으로 사용할 것.
3. **데이터의 복잡한 가공 (파싱 필요 시):** 단순 파일 복사가 아닌, JSON 등의 강력한 텍스트 변환 및 파이프라이닝이 필요할 때만 예외적으로 `node script.js` (fs 연산 포함) 방식을 사용하는 것이 쉘 스크립트보다 안전하다.
4. **설레발 및 독단적 작업 금지:** 사용자가 명시적으로 지시하지 않은 추가 작업(불필요한 예측 생성, 기존 정상 데이터의 자의적인 덮어쓰기 등)을 설레발치며 임의로 막 수행하지 않는다. 오직 지시받은 범위 내에서 정확하고 확실하게 완수하는 데 집중한다. -->