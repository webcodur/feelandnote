# 셀럽 데이터 결손 전수 정비 — 진행·재개 문서

> 시작: 2026-07-31 · 상태: **진행 중**
>
> **2026-08-07 — 텍스트 트랙 전량 소진.** 00:10~11:15(약 11시간, 세션 한도로 03:30~08:09 중단
> 포함) 동안 영문 번역 2,471칸, 영향력·페르소나 249명, 말투·대사 69명을 채웠다.
> 남은 결손은 **얼굴 사진 275명**(이 트랙 밖)과 **근거 부재로 보류한 42건**(명언 31 · 생년 8 ·
> 성별 2 · 국적 1)뿐이다. 보류분은 채우는 것이 아니라 비워 두는 것이 규격이거나, 출처를
> 못 찾아 지어내지 않기로 판정한 항목이다. 전부 `deferred.json` 에 사유가 있다.
>
> 🔴 **"완료"라는 상태는 유지되지 않는다.** 다른 경로로 CELEB이 계속 등록된다 — 이날만 인원이
> 2,426명에서 2,742명으로 늘었고, 영향력·페르소나는 오전에 0을 찍은 뒤 신규 53명이 들어왔다.
> 다음 세션은 "이미 끝난 작업"이라 여기지 말고 감사부터 다시 돌려라.
>
> **2026-08-06 완료 — 직군·국적·성별 3필드.** fiction 포함 전 티어 결손을 정리했다.
> 최종 실측: profession 0 / nationality 3(전부 신원·배경 불명 보류) / gender 2(호랑이·야마타노오로치 —
> 성별 묘사 없는 동물·괴물 보류). 대부분은 DB 직접 반영 + `celeb-fill.ts apply`로 해결,
> 보류 3건은 `deferred.json`에 근거와 함께 기록됐다.

## 무엇을 하는 작업인가

`profiles`(CELEB) 2,426명의 **텍스트 데이터 빈칸**을 룰북 규격에 맞게 채운다. 대상 트랙은
basic(이름·수식어·소개·직군·국적·생몰) · 감상철학 · 영향력 7축 · 페르소나 16항목+종합해설 ·
speech(톤·명언·대사 21개) · 영문(i18n)이다.

**범위 밖**: 가상독백(`virtual_monologue`). 전용 트랙이 따로 있고 한국어 전수는 이미 완료됐다
(`docs/archive/virtual-monologue-quality-overhaul-2026-07.md`).

**결손으로 세지 않는 것**: `fiction` 티어의 생몰(특정 불가가 정상), `fiction`·`relation`
티어의 영문 필드(번역 대상 아님), `death_date`(생존자는 비어 있는 게 정상).

> **26.08.06 변경 — 직군·국적·성별은 fiction 포함 전 티어 결손이다.** 기존에는 fiction의
> 국적을 "특정 불가 정상"으로 제외했으나, 원전(작품·신화) 배경 지역으로 재구성해 채우는
> 규칙으로 바꿨다. 규칙 SSoT는 `celeb-1-basic-profile.md`의 「국적 결손」·「성별」 절,
> 감사는 `audit-celeb-track-gaps.ts`(전 티어 `basic:nationality`·`basic:gender` 검사)를 따른다.

## 구조 — 조사와 반영을 분리했다

에이전트는 **조사만** 하고 로컬 JSON을 남긴다. DB 반영은 스크립트가 결정론적으로 한다.
이렇게 하면 에이전트가 중간에 죽어도 작업물이 남고, 에이전트 정원을 전부 조사에 쓸 수 있다.

```
선점 → 조사 → research/<slug>.json  (에이전트, 여러 대)
                    ↓
        celeb-fill.ts apply-dir --apply  (오케스트레이터, 1회)
```

### 도구 (모두 `sw/web-bo`에서 실행)

| 스크립트 | 역할 |
|---|---|
| `scripts/audit-celeb-track-gaps.ts` | **결손 전수 감사.** 진행 상황의 단일 원천. `--json` 지원 |
| `scripts/audit-celeb-basic-gaps.ts` | basic 필드만 보는 축약 감사 |
| `scripts/claim-celeb-work.ts` | `--worker <레인> --count N --target <묶음>` — 레인별 독립 선점. 중복 선점 차단, lease 30분, 보류 항목 제외. `--target`은 `basic3`(직군·국적·성별, 기본값) / `influence-persona` / `i18n`(영문 번역) / `speech-basic`(수식어·말투·대사 21개) 중 하나이며, 그 트랙 결손자만 뽑는다 |
| `scripts/celeb-fill.ts` | `dump` / `apply --file` / `apply-dir --dir` — **빈칸만 채우는** 조건부 반영. 기본 dry-run, `--apply`로 저장 |
| `scripts/defer-celeb-gap.ts` | 근거 부재 항목 보류 기록. 기록분은 선점 대상에서 빠져 큐 앞단을 막지 않는다 |
| `scripts/build-celeb-wave.ts` | (구) 파도 단위 묶음 생성기. 선점 방식으로 대체됐다 |

### 작업 폴더 `sw/web-bo/.tmp-celeb-fill/`

| 경로 | 내용 |
|---|---|
| `INSTRUCTIONS-influence-persona.md` | **영향력·페르소나 전용 지시서.** 7축·16축 규격(영문 설명 포함), 점수 눈금과 앵커, 무력 8단계, 동점 금지, 한국어 문장 규칙 |
| `INSTRUCTIONS-i18n.md` | **영문 번역 전용 지시서.** 번역 대상 5종, 대사 원문 조회법, 명언 원문 복원 원칙, 말투별 영문 결, 태그 금지 |
| `research/` | 조사 결과 대기분 |
| `research-applied/` | 반영 완료분 (이력). 26.08.07 기준 862건 |
| `claims/` | 선점 잠금. 반영 성공 시 자동 반납 |
| `deferred.json` | 보류 장부 (근거 부재 사유 포함). 26.08.07 기준 12건 |

> basic 3필드 지시서(`INSTRUCTIONS.md`)는 그 트랙이 26.08.06에 끝나 삭제됐다. 다시 필요하면
> 위 두 지시서를 본떠 만든다.
>
> **레인이 작업 없이 끝나면 그 인물의 선점 잠금이 남는다.** 반영 도구는 성공한 인물만 잠금을
> 푼다. 그래서 "이미 채워져 있었다"로 끝난 레인의 잠금은 30분 뒤 만료될 때까지 그 인물을
> 붙잡고, 잔량이 얼마 안 남은 막바지에는 이것 때문에 NO_WORK 가 뜬다. 가동 중인 레인이 전부
> 끝난 것을 확인한 뒤 `rm -f .tmp-celeb-fill/claims/*.json` 으로 회수하면 된다.

## 반영 도구의 안전 규칙

- **빈칸만 채운다.** 기존값이 있는 필드·대사 슬롯은 무조건 보존한다.
- 신규 셀럽 생성 경로가 없다. 기존 인물의 빈칸 채우기 전용.
- `celeb_influence`·`celeb_persona`·`celeb_dialogues` 행이 없으면 만든다.
- `celeb_persona`는 `persona` jsonb만 쓴다(평면 점수 컬럼은 DB 트리거가 동기화).
- 점수 범위 검사: 영향력 0~10 정수, 페르소나 0~100(dispositions는 -100~100).
- 반영 후 DB를 다시 읽어 **왕복 검증**한다. jsonb 키 순서는 무시하고 비교한다.
- 작성자 실수 흡수: 영향력 필드를 `profiles` 아래에 넣으면 제 위치로 옮긴다.
  `speech_tone`·`profession`이 규정 코드가 아니면 그 필드만 빼고 나머지는 반영한다.

## 운영 방법 — 동시 실행 대수가 제약이다

🔴 **서브에이전트 동시 실행을 늘리면 처리량이 늘지 않고 서비스 처리량 제한으로 즉시 실패한다.**
실측(2026-07-31~08-01):

| 동시 대수 | 결과 |
|---|---|
| 40 / 14 / 12 | 시작 직후 차단, 산출 0~9건 |
| 10 / 6 / 4 | 조기 차단, 산출 9~22건 |
| 3 | 완주하기도 하고 차단되기도 함 |
| **1 (레인 1개 × 12단계 순차)** | **매번 완주. 회차당 15~36건 산출** |

🟢 **2026-08-07 재실측 — 6대 동시가 정상 완주했다.** 위 표(07-31~08-01)와 정면으로 어긋난다.
영향력·페르소나 회차에서 레인 6개 × 인물 4~5명을 약 두 시간 동안 쉼 없이 돌렸고, 실패한 레인은
하나도 없었다. 한 레인이 4~5명을 처리하는 데 5~11분이 걸렸다.

**위 표의 "즉시 차단"은 처리량 제한이 아니라 발주 방식 때문일 수 있다.** Claude Code에서
서브에이전트에 **이름을 붙여 발주하면** tmux(리눅스 도구)를 요구하는 무리 실행 방식으로 전환되어
Windows에서 즉시 실패한다. 이름 없이 발주하면 6대가 문제없이 병렬로 돈다. 07-31 실측 당시의
발주 형태는 기록에 남아 있지 않아 단정할 수 없으나, **"동시 대수를 늘리면 실패한다"는 결론을
그대로 믿고 1레인 순차로 돌아가지 마라.** 먼저 6대로 시험하고, 실패하면 그때 줄여라.

그 이전 운용은 **레인 1개 × 순차 12단계**였다. 각 단계가 새 세션이라 컨텍스트가 초기화되고,
스스로 3명을 선점해 처리한 뒤 끝난다. 단계 사이에 다른 레인을 기다리지 않는다.

한 회차 절차:

```bash
cd sw/web-bo
# 1) 레인 1개 × 12단계로 조사 (subagent, model=claude-opus-5)
#    각 단계 프롬프트: "LANE=lane-a. .tmp-celeb-fill/INSTRUCTIONS.md 절차대로 조사만 하라"
# 2) 반영
pnpm exec tsx scripts/celeb-fill.ts apply-dir --dir .tmp-celeb-fill/research          # dry-run
pnpm exec tsx scripts/celeb-fill.ts apply-dir --dir .tmp-celeb-fill/research --apply
# 3) 진행 확인
pnpm exec tsx scripts/audit-celeb-track-gaps.ts
```

중단으로 선점이 남았으면 `rm -rf .tmp-celeb-fill/claims`로 회수한다(lease 30분이면 자동 만료).
장시간 돌릴 때는 `caffeinate -dimsu -t 10800 &`로 절전을 막는다.

## 우선순위

선점기는 `status='active'` 먼저, 그다음 결손 개수 많은 순으로 준다. 공개 노출 중인 인물이
먼저 정비된다.

## 남은 규모 (2026-08-07 11:15 실측 · CELEB 2,742명)

**텍스트 트랙은 이날 전부 소진됐다.** 남은 것은 얼굴 사진(이 트랙 밖)과 근거 부재로 보류한 항목뿐이다.

| 필드 | 이날 새벽(00:10) | 09:49 | 11:15 | 비고 |
|---|---:|---:|---:|---|
| `i18n:*_exp_en` (영향력 설명 7축) | 762 | 277 | **0** | |
| `i18n:quote_en` (명언) | 628 | 224 | **0** | |
| `i18n:lines_en` (대사 21개) | 612 | 205 | **0** | |
| `i18n:bio_en` (소개) | 449 | 34 | **0** | |
| `i18n:title_en` (수식어) | 20 | 68 | **0** | |
| `speech:tone`·`dialogue_row` | 69 | 6 | **0** | |
| `influence:row`·`persona:row` | 249 | 53 | **1** | 클로드 라이언 — 처리 중 |
| `basic:birth_date` | — | 13 | **8** | 5명은 출처 확인해 채움, 8명은 근거 없어 보류 |
| `speech:quote` (명언 한국어) | — | 25 | **31** | 확인된 발언이 없으면 비우는 게 규격. 채우지 않는다 |
| `basic:gender`·`basic:nationality` | — | 3 | **3** | 전부 보류 장부 기록분 |
| `basic:avatar_url` | 239 | 287 | **275** | 얼굴 사진. 텍스트 조사와 성격이 달라 이 트랙 밖 |

🔴 **이 표는 움직이는 표적이다.** 다른 경로로 CELEB이 계속 등록되고 있어(08-06 밤~08-07 오전에만
수백 명 — 이날 하루 전체 인원이 2,426명에서 2,742명으로 늘었다) 채운 만큼 새 결손이 생긴다.
영향력·페르소나는 08-07 오전에 결손 0을 찍었으나 그 뒤 신규 53명이 들어왔고, 그 53명을 처리하는
동안 다시 48명이 남았다. **"전부 끝냈다"는 상태는 유지되지 않으므로, 목표를 "0 만들기"가 아니라
"유입 속도보다 빠르게 채우기"로 잡아라.**

**어느 트랙도 집어가지 않는 결손이 있다.** `basic:birth_date`는 `basic3`·`i18n`·
`influence-persona`·`speech-basic` 넷 중 어느 묶음에도 없어 선점기가 영원히 뽑지 않는다.
이런 항목은 감사 결과에서 명단을 직접 뽑아 발주해야 한다.

> 이전 기록(2026-08-01): 결손 587명, 영향력·페르소나는 행 자체가 없는 인물이 300명대.

```bash
pnpm exec tsx scripts/audit-celeb-track-gaps.ts   # 최신 수치는 항상 이 명령이 정본
```

## 알게 된 함정

- **근거 부재 항목이 큐 앞단을 막는다.** 활성 인물의 생년 미상 같은 항목은 매번 먼저 뽑혀
  작업자가 같은 헛수고를 반복한다. `defer-celeb-gap.ts`로 반드시 기록해야 큐가 전진한다.
- **`celeb_persona.persona`에는 `rationale_ko`·`rationale_en`(종합 해설)이 있다.** 그룹 4종만
  있는 줄 알고 만들면 반영이 거부된다.
- **jsonb는 키 순서를 보존하지 않는다.** 왕복 검증을 문자열 비교로 하면 정상 반영이 실패로 찍힌다.
- **반영이 잦으면 같은 인물을 두 레인이 잡는다.** 반영 도구는 성공한 인물의 선점을 풀어 준다.
  그런데 선점기가 결손 감사를 돌리는 데 30초 남짓 걸려서, 그 사이에 반영이 끝나면 이미 채워진
  인물을 새 레인이 집어간다. 실제로 08-07 회차에서 여러 번 발생했다. 손실은 크지 않다 —
  지시서가 "선점 후 `dump`로 확인해 이미 채워졌으면 건너뛰고 다시 선점하라"고 시키기 때문에
  에이전트가 1~2분 안에 알아서 회복한다. 반영 주기를 3~4회차에 한 번으로 늘리면 빈도가 준다.
- **한 세션이 띄울 수 있는 서브에이전트는 200개가 상한이다.** 6~7대를 계속 채워 돌리면 대략
  반나절 만에 닿는다(08-07 실측: 00:10~09:49에 소진). 닿으면 그 세션에서는 더 못 띄우고
  "Subagent spawn limit reached" 로 즉시 실패한다. 작업 상태는 DB와 작업 폴더에 남으므로
  새 세션에서 그대로 이어가면 되고, 미리 늘리려면 `CLAUDE_CODE_MAX_SUBAGENTS_PER_SESSION` 을 올린다.
  08-07에 `.claude/settings.local.json` 의 `env` 에 이 값을 1000으로 넣어 해결했다.
  **재시작 없이 즉시 적용됐다** — 상한에 닿은 세션을 버리고 새로 시작할 필요가 없다.
- 🔴 **번역 결손은 "원문이 있는데 영문만 빈" 것만 세야 한다.** 감사 코드가 한국어 명언이 없는
  인물까지 `i18n:quote_en` 결손으로 세고 있었다. 그래서 번역 담당이 채울 수 없는 인물을 반복해
  선점했고(선점 8명 중 실작업 1~2명), 결손 수치도 부풀려졌다. 08-07에 감사를 고쳐
  **한국어 원문이 있을 때만 영문 결손으로 세도록** 했다 — 명언 69→29, 대사도 같은 방식으로
  자리 단위 대조. 결손 정의를 세울 때 "이 항목을 채울 재료가 실제로 있는가"를 조건에 넣어라.
- 🔴 **한 트랙이 다른 트랙의 결손을 만들어내고 있었다.** 영향력·페르소나 지시서가 영문 설명
  (`*_exp_en`)을 "영문 번역 트랙이 따로 채운다"며 일부러 비워 두게 했다. 그래서 영향력 행을
  새로 만들 때마다 번역 결손이 7칸씩 생겼다 — 08-07에 영향력 영문 결손이 5명까지 줄었다가
  영향력 회차가 돌자 25명으로 되레 늘었다. 같은 인물을 두 에이전트가 각각 조사하는 셈이다.
  08-07에 지시서를 고쳐 **한국어 설명을 쓴 에이전트가 영문 한 줄도 함께 쓰도록** 했다.
  방금 자기가 쓴 문장을 옮기는 것뿐이라 추가 조사가 없다. **트랙을 쪼갤 때는 "이 트랙이 다른
  트랙의 일감을 만들어내지 않는가"를 먼저 따져라.**
- **레인이 "이미 다 채워져 있었다"로 자주 돌아오면 그 트랙이 소진된 것이다.** 08-07 번역 회차
  후반에 선점 8명 중 실제 작업 대상이 1~2명으로 떨어졌다. 선점기는 그 트랙 결손자만 뽑는데도
  이렇게 되는 이유는, 결손으로 잡히는 항목의 **한국어 원문이 없어 번역 자체가 불가능**하기
  때문이다(명언 69명 중 25명이 그렇다). 이 신호가 보이면 레인을 그 트랙에서 빼고 잔량이 많은
  트랙으로 옮겨라. 실제 잔량은 감사 수치가 아니라 "원문이 있는데 번역만 빈" 건수로 세야 한다.
- **결손 감사가 조회 제한 시간에 걸릴 수 있다.** `celeb_persona.persona`·`celeb_dialogues.lines` 는
  인물당 수 KB짜리 jsonb라 1,000행씩 읽으면 여러 레인이 동시에 감사를 돌릴 때 터진다.
  08-07에 그 두 테이블만 200행 단위로 낮춰 해결했다(`audit-celeb-track-gaps.ts`). 앞으로 데이터가
  더 불면 이 값을 더 낮춰라.
- **선점 대상에서 「채울 근거가 없는 게 정상인 항목」을 빼라.** `speech:quote`(명언)는 확인된 실제
  발언이 없으면 비워 두는 것이 규격인데도 결손으로는 계속 잡힌다. 그래서 그 한 칸만 빈 인물이
  큐 앞단을 반복해 차지했다(대상 풀 35명 중 실제 작업 대상은 12명뿐이었다). `speech-basic` 묶음에서
  `speech:quote` 를 뺀 뒤 풀이 정확해졌다.
- **서브에이전트에게 "선점 명령은 백그라운드로 돌리지 마라"고 못박아라.** 명령이 40초쯤 걸린다고
  알려주면 일부 에이전트가 백그라운드로 띄우고 폴링하다가 아무 작업도 못 하고 끝난다(08-07에 2대 손실).
- **조사 에이전트에게 룰북 전체를 읽히지 마라.** 룰북 합계 114KB라 여러 대가 동시에 읽으면
  처리량 한도를 밀어올린다. 규격 요약을 지시서에 넣고 룰북은 예외 상황에만 열게 했다.
- **26.07.31 22:13~22:23(UTC 13:13~13:23)에 CELEB 157명이 신규 생성됐다.** 아이돌·배우
  묶음이며 이 정비 작업과 무관한 별도 등록 경로다(같은 날 19:46에도 같은 형태가 있었다).
  이 인물들은 basic은 갖췄으나 영향력·페르소나·대사 행이 없어 이번 정비 대상에 들어간다.
